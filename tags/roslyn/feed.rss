<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:media="http://search.yahoo.com/mrss/"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Roslyn: The .NET Compiler Platform on Daily DevOps &amp; .NET</title>
    <link>https://daily-devops.net/tags/roslyn/</link>
    <description>Recent content in Roslyn: The .NET Compiler Platform on Daily DevOps &amp; .NET</description>
    <language>en</language>
    <copyright>© 2026 Daily DevOps &amp; .NET</copyright>
    <lastBuildDate>Mon, 24 Aug 2026 00:52:00 +0000</lastBuildDate>
    <image>
      <url>https://daily-devops.net/images/logo_hu_295faa9e530b1c14.png</url>
      <title>Roslyn: The .NET Compiler Platform on Daily DevOps &amp; .NET</title>
      <link>https://daily-devops.net/tags/roslyn/</link>
      <width>144</width>
      <height>144</height>
    </image>
    <atom:link type="application/rss+xml" href="https://daily-devops.net/tags/roslyn/feed.rss" hreflang="en" rel="self"/>
    <atom:link type="text/html" href="https://daily-devops.net/tags/roslyn/" hreflang="en" rel="alternate"/>
    <atom:link type="application/atom+xml" href="https://daily-devops.net/tags/roslyn/feed.atom" hreflang="en" rel="alternate"/>
    <atom:link type="application/feed+json" href="https://daily-devops.net/tags/roslyn/feed.json" hreflang="en" rel="alternate"/>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/analyzer-dogfooding-roslyn-mef-pitfall/</guid>
      <link>https://daily-devops.net/posts/analyzer-dogfooding-roslyn-mef-pitfall/</link>
      <title>My Analyzer Shipped Without Checking Its Own Source</title>
      <pubDate>Tue, 11 Aug 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>NetEvolve.Analyzer enforces null-check idioms and file organization on other people’s code, but had never run against its own. Fixing that surfaced 39 warnings, a silent Visual Studio MEF failure, and a follow-up NE0008 bug where DateOnly and TimeOnly got flagged blind to which target framework actually has them, the same multi-targeting blindness in a different corner of the codebase.</description>
      <content:encoded><![CDATA[<p>I maintain <a href="https://github.com/dailydevops/analyzer" target="_blank" rel="noopener external noreferrer">NetEvolve.Analyzer</a>, a Roslyn analyzer that tells other people&rsquo;s C# code to use <code>is null</code> instead of <code>== null</code>, keep one type per file, and stop nesting <code>#region</code> blocks nobody reads. It ships four separate builds to cover four Roslyn API versions. It has automatic code fixes. It has decent test coverage.</p>
<p>It had never run against its own source code.</p>
<p>That is not a hypothetical embarrassment I am inventing for a hook. I merged <a href="https://github.com/dailydevops/analyzer/pull/49" target="_blank" rel="noopener external noreferrer">PR #49</a> on August 5, 2026, specifically to fix it, and the first thing that happened when I pointed the analyzer at itself was 39 warnings across three rules I had written and shipped. The tool that tells you your code has problems had problems it had never told itself about.</p>




<h2 id="why-an-analyzer-skips-its-own-medicine"><a href="/posts/analyzer-dogfooding-roslyn-mef-pitfall/#why-an-analyzer-skips-its-own-medicine" title="Why an Analyzer Skips Its Own Medicine">Why an Analyzer Skips Its Own Medicine</a></h2>
<p>The reason is mechanical, not laziness. <code>NetEvolve.Analyzer</code> multi-targets four Roslyn API versions (4.4.0, 4.7.0, 4.14.0, 5.6.0) because consumers on older SDKs need an analyzer that loads against their compiler, not the newest one. Each version is its own <code>.csproj</code>, compiling the same shared source under a different <code>RoslynApiVersion</code> and, until now, the same <code>AssemblyName</code>.</p>
<p>If you want a Roslyn analyzer project to analyze itself, the obvious move is to reference itself as an <code>Analyzer</code> item. Roslyn analyzers run as MSBuild <code>Analyzer</code> items, not <code>ProjectReference</code> items, specifically so the compiler can load the diagnostic DLL without pulling it into the build&rsquo;s dependency graph as a regular assembly reference. But a project cannot reference its own output as an analyzer before that output exists. That is a build-ordering cycle, and MSBuild will not resolve it for you.</p>
<p>The fix in PR #49 uses the fact that there are four sibling projects compiling the same source:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="c">&lt;!-- NetEvolve.Analyzer.Build.props --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;ItemGroup</span> <span class="na">Label=</span><span class="s">&#34;Dogfooding: apply this analyzer&#39;s own rules to its own source&#34;</span>
</span></span><span class="line"><span class="cl">           <span class="na">Condition=</span><span class="s">&#34; &#39;$(MSBuildProjectName)&#39; != &#39;NetEvolve.Analyzer.Dogfood&#39; &#34;</span><span class="nt">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;ProjectReference</span> <span class="na">Include=</span><span class="s">&#34;NetEvolve.Analyzer.Dogfood.csproj&#34;</span>
</span></span><span class="line"><span class="cl">                    <span class="na">OutputItemType=</span><span class="s">&#34;Analyzer&#34;</span>
</span></span><span class="line"><span class="cl">                    <span class="na">ReferenceOutputAssembly=</span><span class="s">&#34;false&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/ItemGroup&gt;</span>
</span></span></code></pre></div><p><code>OutputItemType=&quot;Analyzer&quot;</code> tells MSBuild to feed the referenced project&rsquo;s output DLL into the compiler as an analyzer, not as a regular assembly reference. <code>ReferenceOutputAssembly=&quot;false&quot;</code> stops the compiler from also linking against it as a library. The <code>Condition</code> excludes the dogfood project from referencing itself. Each of the four real variants gets checked by a sibling build, never by itself, so there is no cycle: <code>NetEvolve.Analyzer.Roslyn4_4</code> depends on <code>NetEvolve.Analyzer.Dogfood</code>, never the reverse.</p>
<p>Building the Roslyn 4.4 variant with this wired up surfaced 39 warnings against three rules: <a href="https://github.com/dailydevops/analyzer/blob/main/docs/rules/NE0007.md" target="_blank" rel="noopener external noreferrer">NE0007</a> (use <code>&lt;see langword&gt;</code> for keywords in doc comments instead of <code>&lt;c&gt;true&lt;/c&gt;</code>), NE0008 (use <code>&lt;see cref&gt;</code> for native types instead of <code>&lt;c&gt;string&lt;/c&gt;</code>), and <a href="https://github.com/dailydevops/analyzer/blob/main/docs/rules/NE0009.md" target="_blank" rel="noopener external noreferrer">NE0009</a> (methods with a <code>CancellationToken</code> parameter must check it before doing real work). Thirty-nine warnings, in a codebase whose entire job is telling other people to fix exactly this class of problem, and the build stayed green throughout because these are warnings, not errors. Nothing forced anyone to look at them. That is precisely the failure mode dogfooding exists to catch: a tool that would flag the issue in your codebase but never flags it in its own, because nobody wired it up to check.</p>




<h2 id="the-silent-failure-referencing-yourself-creates"><a href="/posts/analyzer-dogfooding-roslyn-mef-pitfall/#the-silent-failure-referencing-yourself-creates" title="The Silent Failure Referencing Yourself Creates">The Silent Failure Referencing Yourself Creates</a></h2>
<p>Getting the diagnostics to fire was the easy half. The code fixes, the actual value proposition of an analyzer with a lightbulb icon, quietly stopped working, and nothing told me.</p>
<p>The first attempt referenced the existing <code>NetEvolve.Analyzer.Roslyn4_14</code> build (the 4.14 baseline) as the <code>Analyzer</code> item for the sibling projects. Diagnostics appeared in the editor exactly as expected. Code fixes did not. No error, no warning, no red squiggle explaining why: the lightbulb simply never appeared for rules that unambiguously should have offered one.</p>
<p>The cause is Visual Studio&rsquo;s MEF composition for analyzer and code-fix providers. Every one of the four real variants shares the assembly name <code>NetEvolve.Analyzer</code>, because that is the name consumers expect in their <code>PackageReference</code>. When two same-named assemblies from different paths (the NuGet-facing build and the one loaded as a dogfooding analyzer) end up in the same solution&rsquo;s MEF catalog, Visual Studio&rsquo;s composition silently drops the <code>CodeFixProvider</code> exports for the duplicate. <code>DiagnosticAnalyzer</code> exports survive; <code>CodeFixProvider</code> exports do not. You get the warning, not the fix, and there is no log line pointing at MEF as the culprit.</p>
<p>The instinctive fix, override the assembly name on the reference itself, does not work either:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="c">&lt;!-- Does not work: Visual Studio&#39;s Solution Build Manager builds each
</span></span></span><span class="line"><span class="cl"><span class="c">     project once and will not build a second, differently-named
</span></span></span><span class="line"><span class="cl"><span class="c">     instance of the same project file. --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;ProjectReference</span> <span class="na">Include=</span><span class="s">&#34;NetEvolve.Analyzer.Roslyn4_14.csproj&#34;</span>
</span></span><span class="line"><span class="cl">                  <span class="na">Properties=</span><span class="s">&#34;AssemblyName=NetEvolve.Analyzer.Dogfood&#34;</span> <span class="nt">/&gt;</span>
</span></span></code></pre></div><p><code>dotnet build</code> resolves its own reference graph and can build parameterized instances of the same project under different property values. Visual Studio&rsquo;s Solution Build Manager does not work that way: it builds each project in the solution once, under whatever <code>AssemblyName</code> its <code>.csproj</code> already declares. A <code>Properties=</code> override on the reference is invisible to it.</p>
<p>So PR #49 adds a fifth project, <code>NetEvolve.Analyzer.Dogfood.csproj</code>, whose only job is to compile the shared source under a genuinely distinct assembly name:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="nt">&lt;Project&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;RoslynApiVersion&gt;</span>5.6.0<span class="nt">&lt;/RoslynApiVersion&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;IsPackable&gt;</span>false<span class="nt">&lt;/IsPackable&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="c">&lt;!-- BaseIntermediateOutputPath/BaseOutputPath must be set before
</span></span></span><span class="line"><span class="cl"><span class="c">       Sdk.props is imported (MSB3539), so this project can&#39;t use the
</span></span></span><span class="line"><span class="cl"><span class="c">       &lt;Project Sdk=&#34;Microsoft.NET.Sdk&#34;&gt; shorthand like its siblings. --&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;BaseIntermediateOutputPath&gt;</span>obj\$(MSBuildProjectName)\<span class="nt">&lt;/BaseIntermediateOutputPath&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;BaseOutputPath&gt;</span>bin\$(MSBuildProjectName)\<span class="nt">&lt;/BaseOutputPath&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;Import</span> <span class="na">Project=</span><span class="s">&#34;Sdk.props&#34;</span> <span class="na">Sdk=</span><span class="s">&#34;Microsoft.NET.Sdk&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;Import</span> <span class="na">Project=</span><span class="s">&#34;NetEvolve.Analyzer.Build.props&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="c">&lt;!-- Overrides the AssemblyName set by Build.props: referenced only
</span></span></span><span class="line"><span class="cl"><span class="c">         as an Analyzer item, never packed or shipped. --&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;AssemblyName&gt;</span>NetEvolve.Analyzer.Dogfood<span class="nt">&lt;/AssemblyName&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;Import</span> <span class="na">Project=</span><span class="s">&#34;Sdk.targets&#34;</span> <span class="na">Sdk=</span><span class="s">&#34;Microsoft.NET.Sdk&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/Project&gt;</span>
</span></span></code></pre></div><p>It targets Roslyn 5.6.0, the newest baseline, so its diagnostics reflect the latest analyzer API surface. It sets <code>IsPackable=false</code> (added in a follow-up commit after the first push, because the project&rsquo;s default packing behavior would otherwise have put an internal build artifact into the NuGet package) so it never ships. And it exists purely as an <code>Analyzer</code> reference target for the other four, none of which build it as a library, none of which risk a cycle, because none of them are it.</p>
<p><strong>If your Roslyn analyzer multi-targets several API versions and shares an assembly name across builds, and you want it to check its own source, do not reuse one of the shipping variants as the analyzer reference.</strong> Give the self-check a dedicated project with its own <code>AssemblyName</code>. It costs one small <code>.csproj</code> and saves you a debugging session where the analyzer looks like it is half-broken and nothing in the IDE tells you why.</p>




<h2 id="what-got-fixed-and-what-is-deliberately-still-open"><a href="/posts/analyzer-dogfooding-roslyn-mef-pitfall/#what-got-fixed-and-what-is-deliberately-still-open" title="What Got Fixed, and What Is Deliberately Still Open">What Got Fixed, and What Is Deliberately Still Open</a></h2>
<p>The first commit in PR #49 only wired up the reference and confirmed the build stayed green with 39 warnings outstanding: the PR description at that point explicitly said the findings were &ldquo;not fixed in this PR, follow-up.&rdquo; That held for exactly one more commit. Once the dedicated Dogfood project made the code fixes actually fire, applying NE0007 across the doc comments was one command, and standardizing on <code>&lt;see cref&gt;</code> (NE0008) and honoring cancellation tokens at the start of async methods (NE0009) landed in the same pass:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Before, in CSharpKeywords.cs: NE0007 and NE0008 both firing on the same doc comment</span>
</span></span><span class="line"><span class="cl"><span class="c1">// (em dash in the real source replaced with a comma here for house style; wording otherwise verbatim)</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// prefix (&lt;c&gt;@class&lt;/c&gt;). Native type names (&lt;c&gt;bool&lt;/c&gt;, &lt;c&gt;byte&lt;/c&gt;, &lt;c&gt;char&lt;/c&gt;, ...) are</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// intentionally excluded, referencing a type by its bare name in a doc comment is idiomatic and not</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// the keyword-in-&lt;c&gt;&amp;lt;c&amp;gt;&lt;/c&gt; mistake this list exists to catch. &lt;c&gt;void&lt;/c&gt; is kept, since it</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// After, applied by the code fix</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// prefix (&lt;c&gt;@class&lt;/c&gt;). Native type names (&lt;see cref=&#34;bool&#34;/&gt;, &lt;see cref=&#34;byte&#34;/&gt;, &lt;see cref=&#34;char&#34;/&gt;, ...) are</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// intentionally excluded, referencing a type by its bare name in a doc comment is idiomatic and not</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// the keyword-in-&lt;c&gt;&amp;lt;c&amp;gt;&lt;/c&gt; mistake this list exists to catch. &lt;see langword=&#34;void&#34;/&gt; is kept, since it</span>
</span></span></code></pre></div><div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Before, in RequireCancellationCheckCodeFixProvider.cs: NE0009 firing on its own fix provider</span>
</span></span><span class="line"><span class="cl"><span class="kd">private</span> <span class="kd">static</span> <span class="kd">async</span> <span class="n">Task</span><span class="p">&lt;</span><span class="n">Document</span><span class="p">&gt;</span> <span class="n">InsertCheckAsync</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">Document</span> <span class="n">document</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">MethodDeclarationSyntax</span> <span class="n">method</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">CancellationToken</span> <span class="n">cancellationToken</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="n">root</span> <span class="p">=</span> <span class="p">(</span><span class="k">await</span> <span class="n">document</span><span class="p">.</span><span class="n">GetSyntaxRootAsync</span><span class="p">(</span><span class="n">cancellationToken</span><span class="p">).</span><span class="n">ConfigureAwait</span><span class="p">(</span><span class="kc">false</span><span class="p">))!;</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// ...</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// After</span>
</span></span><span class="line"><span class="cl"><span class="kd">private</span> <span class="kd">static</span> <span class="kd">async</span> <span class="n">Task</span><span class="p">&lt;</span><span class="n">Document</span><span class="p">&gt;</span> <span class="n">InsertCheckAsync</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">Document</span> <span class="n">document</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">MethodDeclarationSyntax</span> <span class="n">method</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">CancellationToken</span> <span class="n">cancellationToken</span>
</span></span><span class="line"><span class="cl"><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">cancellationToken</span><span class="p">.</span><span class="n">ThrowIfCancellationRequested</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="n">root</span> <span class="p">=</span> <span class="p">(</span><span class="k">await</span> <span class="n">document</span><span class="p">.</span><span class="n">GetSyntaxRootAsync</span><span class="p">(</span><span class="n">cancellationToken</span><span class="p">).</span><span class="n">ConfigureAwait</span><span class="p">(</span><span class="kc">false</span><span class="p">))!;</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// ...</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Twenty-some files across <code>Documentation/</code>, <code>Maintainability/</code>, and <code>Usage/</code> picked up one or both fixes: <code>CSharpKeywords.cs</code>, <code>NullCheckOperand.cs</code>, the various <code>CodeFixProvider</code> classes, <code>RequireCancellationCheckAnalyzer.cs</code>. Coverage landed at 94.42%, every changed line covered, 8 CI checks green. Not a rewrite. A tool correcting itself with its own tooling, which is a more convincing demonstration than any amount of README copy about &ldquo;reliable code fixes.&rdquo; Whatever in the original 39 the code fixes could not resolve mechanically is still open, follow-up work, same as the PR description always said.</p>




<h2 id="the-follow-up-bug-dogfooding-was-supposed-to-catch"><a href="/posts/analyzer-dogfooding-roslyn-mef-pitfall/#the-follow-up-bug-dogfooding-was-supposed-to-catch" title="The Follow-Up Bug Dogfooding Was Supposed to Catch">The Follow-Up Bug Dogfooding Was Supposed to Catch</a></h2>
<p>It caught one within hours. <a href="https://github.com/dailydevops/analyzer/pull/52" target="_blank" rel="noopener external noreferrer">PR #52</a>, merged the same day as #49, is a direct consequence of pointing NE0008 at the analyzer&rsquo;s own multi-targeted source.</p>
<p>NE0008 hardcoded <code>DateOnly</code> and <code>TimeOnly</code> into its list of always-recognized BCL type names, next to <code>string</code> and <code>Guid</code>, and flagged <code>&lt;c&gt;DateOnly&lt;/c&gt;</code> in a doc comment regardless of what the consuming project actually targets. Both types were introduced in .NET 6. A project targeting <code>netstandard2.0</code> has no such type to <code>cref</code>, so rewriting the doc comment to <code>&lt;see cref=&quot;DateOnly&quot;/&gt;</code> there is not a style improvement, it is a compile error waiting to happen.</p>
<p>Two distinct gaps, not one. First, the analyzer flagged the doc comment even when the current compilation could not resolve the type at all: a plain hardcoded-name check, no verification against the compiler in front of it. Second, and specific to multi-targeted projects: the analyzer runs once per target framework and can correctly decide, per compilation, whether the type exists there. But the code fix, once triggered, rewrote the doc comment for every target framework in one shot. If one sibling TFM in the project&rsquo;s <code>TargetFrameworks</code> list predates the type, the rewritten <code>&lt;see cref=&quot;DateOnly&quot;/&gt;</code> fails to resolve on that sibling and breaks the build for a framework the fix never even looked at.</p>
<p>That second failure mode is the same shape as the MEF collision earlier in this post: a check that is correct for the compilation directly in front of it, wrong for a sibling target it never considers, because nothing forced it to look sideways. <code>NetEvolve.Analyzer</code> itself multi-targets four Roslyn API versions; running NE0008 against that source is exactly the scenario where a hardcoded BCL-type-name list, blind to which framework it is currently compiling against, was always going to fall over.</p>
<p>The fix moves <code>DateOnly</code>/<code>TimeOnly</code> out of the unconditional well-known-types list into <code>ConditionalBclTypeMinimumVersions</code>, a name-to-minimum-version map (not hardcoded to .NET 6, so a future conditionally-available BCL type with a different introduction version is not misclassified the same way). The analyzer only treats the type as recognized once <code>Compilation.GetTypeByMetadataName</code> confirms it actually resolves in the current compilation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// NativeTypeCrefAnalyzer.cs, conceptually: don&#39;t trust the name list alone</span>
</span></span><span class="line"><span class="cl"><span class="k">if</span> <span class="p">(</span><span class="n">ConditionalBclTypeMinimumVersions</span><span class="p">.</span><span class="n">ContainsKey</span><span class="p">(</span><span class="n">typeName</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">&amp;&amp;</span> <span class="n">compilation</span><span class="p">.</span><span class="n">GetTypeByMetadataName</span><span class="p">(</span><span class="s">$&#34;System.{typeName}&#34;</span><span class="p">)</span> <span class="k">is</span> <span class="kc">null</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Not in scope for this compilation: this TFM predates the type. Don&#39;t flag.</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The code fix gets a second, separate check: before rewriting to <code>&lt;see cref=&quot;...&quot;&gt;</code>, it inspects the project&rsquo;s full <code>TargetFrameworks</code> list and withholds the rewrite if any sibling framework predates the type, even though the diagnostic itself still fires correctly for the TFM that does have it. Getting that list into the analyzer required its own small fix: MSBuild&rsquo;s <code>$(TargetFrameworks)</code> uses semicolons to separate values, but Roslyn&rsquo;s <code>AnalyzerConfig</code> format treats an unescaped <code>;</code> as a comment leader and silently truncates the property at the first entry. <code>NetEvolve.Analyzer.props</code> now re-joins the list with commas before exposing it as <code>NetEvolveAnalyzerTargetFrameworks</code>, a <code>CompilerVisibleProperty</code>.</p>
<p>Five new test cases pin down the boundary: a diagnostic fires against net8.0 reference assemblies, none fires against <code>netstandard2.0</code>, the same source analyzed independently against two TFMs shows no cached state leaking between them, the fix is withheld when a multi-targeted project names an incompatible sibling framework, and it still applies when every listed TFM supports the type. Coverage: 94.37%, 38 new lines.</p>
<p><strong>The general lesson, if you are writing any analyzer rule that references a specific BCL type by name: check <code>Compilation.GetTypeByMetadataName</code>, not a hardcoded string list, and if your rule also ships a code fix, check the project&rsquo;s full <code>TargetFrameworks</code>, not just the one compilation the analyzer happened to run against.</strong> <code>DateOnly</code>/<code>TimeOnly</code> versus .NET 6 is one instance. Any BCL type introduced after your oldest supported TFM is the same bug waiting for the next dogfooding pass, or the next multi-targeted consumer who reports it, to find.</p>




<h2 id="why-this-should-change-how-you-evaluate-the-analyzer"><a href="/posts/analyzer-dogfooding-roslyn-mef-pitfall/#why-this-should-change-how-you-evaluate-the-analyzer" title="Why This Should Change How You Evaluate the Analyzer">Why This Should Change How You Evaluate the Analyzer</a></h2>
<p>None of this changes what the analyzer does for a consumer: <code>PackageReference</code> it as a dev dependency, it adds zero runtime assemblies, it runs against <code>net8.0</code>, <code>net9.0</code>, and <code>net10.0</code> targets, and its file-organization rules can be opted out of via MSBuild properties if you disagree with one-type-per-file as a hard rule. That part was already true.</p>
<p>What changes is what you can infer about whether the rules are any good. A linter that has never been pointed at its own several-thousand-line codebase is a linter whose maintainer has only ever seen it work on other people&rsquo;s problems. The gap between &ldquo;passes my unit tests&rdquo; and &ldquo;survives contact with a real codebase, including mine&rdquo; is exactly where analyzer rules turn out to be too aggressive, miss obvious cases, or (as happened here) ship a code-fix provider that silently does not fire under a condition nobody tested for. Dogfooding does not prove the rules are correct. It proves someone actually ran them somewhere that matters to them, which is a lower bar than correctness and a higher bar than most tooling clears.</p>
<p>The MEF pitfall is also a useful data point independent of this analyzer specifically. If you are building or evaluating any Roslyn analyzer that multi-targets several API versions under a shared <code>AssemblyName</code>, and you see diagnostics appear without their code fixes, check whether two same-named analyzer assemblies from different paths have landed in the same MEF catalog before you assume the <code>CodeFixProvider</code> itself is broken. It is a two-hour investigation if you know to look there, and an open-ended one if you do not.</p>
<p><code>NetEvolve.Analyzer</code> is a young project: no meaningful star count, a small commit history, one contributor. I would not point at PR #49 as evidence of &ldquo;community momentum,&rdquo; because there is none to point at yet, and pretending otherwise would be dishonest about a repository I run myself. What I would point at is the shape of the fixes, which is my own read, not a metric. I have seen this exact class of bug, an analyzer rule that knows a BCL type by name but not by which target frameworks actually have it, play out before in more established Roslyn analyzer projects, usually surfaced by a confused user filing an issue rather than by the maintainer finding it first. PR #52 is that same failure mode caught within hours of PR #49 landing, by the maintainer&rsquo;s own dogfooding pass, on the maintainer&rsquo;s own multi-targeted codebase, not by somebody else&rsquo;s bug report eighteen months from now. Multi-targeting plus a newer BCL type is not a one-off mistake; it is a recurring class of bug for any analyzer that spans more than one target framework or Roslyn version. Seeing that pattern show up here this early, and getting closed the same day, is the good sign. Not that the project has traction. That whoever runs it went looking for the multi-targeting failure mode before it became somebody else&rsquo;s confusing support ticket.</p>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/netevolve.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/netevolve.png" medium="image"/>
      <category>dotnet</category>
      <category>csharp</category>
      <category>analyzers</category>
      <category>nuget</category>
      <category>opensource</category>
      <category>codequality</category>
      <category>bestpractices</category>
      <category>softwareengineering</category>
      <category>roslyn</category>
    </item>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/incremental-source-generators-packaging/</guid>
      <link>https://daily-devops.net/posts/incremental-source-generators-packaging/</link>
      <title>Incremental Source Generators Done Right: Ship It Without Breaking Consumers</title>
      <pubDate>Thu, 30 Jul 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>Parts 1 through 3 of this series made the generator correct, incremental, and proven by tests. None of it reaches a single consumer if the NuGet package is laid out wrong — and the failure modes are silent: a DLL in lib/ instead of analyzers/dotnet/cs simply never loads, a missing dependency throws only inside the compiler, and a marker attribute delivered carelessly breaks InternalsVisibleTo in ways users cannot diagnose. This final part covers the packaging contract: the netstandard2.0 rule, the package layout, dependency bundling, attribute delivery, debugging the shipped bits, and the analyzer hygiene rules that keep you honest.</description>
      <content:encoded><![CDATA[<p>This is part 4 of 4, and it is the part where everything can still go wrong. <a href="/posts/incremental-source-generators-done-right/">Part 1</a> established the equality contract, <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2</a> shaped the pipeline, and <a href="/posts/incremental-source-generators-testing/">part 3</a> proved the cache hits with a test the driver cannot lie to. At this point the generator is correct, incremental, and regression-protected — on your machine.</p>
<p>Then you pack it, someone installs the package, and nothing happens. No generated code, no error, no diagnostic. Or worse: it works in <code>dotnet build</code> and silently does nothing in Visual Studio. Every one of these failures is a packaging failure, and every one of them is invisible in your own repository because a <code>ProjectReference</code> with <code>OutputItemType=&quot;Analyzer&quot;</code> hides all of it. The package is the product. Let us get it right.</p>




<h2 id="netstandard20-is-non-negotiable"><a href="/posts/incremental-source-generators-packaging/#netstandard20-is-non-negotiable" title="netstandard2.0 Is Non-Negotiable">netstandard2.0 Is Non-Negotiable</a></h2>
<p>Your generator is a plugin loaded <em>into the compiler process</em>, and you do not control where that process runs. <code>dotnet build</code> hosts Roslyn on modern .NET. Visual Studio hosts it on .NET Framework 4.7.2+. MSBuild.exe in classic CI pipelines: also .NET Framework. The only TFM loadable in all of these hosts is <code>netstandard2.0</code>. Target anything else and your generator works in exactly the environments you tested and fails in the rest — typically by not loading at all, with a warning buried in the build output that nobody reads.</p>
<p>Here is the project file for <code>AutoToString.Generators</code> — the package that ships part 2&rsquo;s <code>AutoToStringGenerator</code> — and the minimum viable configuration I would accept:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="nt">&lt;Project</span> <span class="na">Sdk=</span><span class="s">&#34;Microsoft.NET.Sdk&#34;</span><span class="nt">&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;TargetFramework&gt;</span>netstandard2.0<span class="nt">&lt;/TargetFramework&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;LangVersion&gt;</span>latest<span class="nt">&lt;/LangVersion&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;Nullable&gt;</span>enable<span class="nt">&lt;/Nullable&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;ImplicitUsings&gt;</span>enable<span class="nt">&lt;/ImplicitUsings&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c">&lt;!-- Enables the F5 debugging experience, see below. --&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;IsRoslynComponent&gt;</span>true<span class="nt">&lt;/IsRoslynComponent&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c">&lt;!-- Opts into RS1035 and friends: bans APIs that are
</span></span></span><span class="line"><span class="cl"><span class="c">         unreliable or forbidden inside the compiler. --&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;EnforceExtendedAnalyzerRules&gt;</span>true<span class="nt">&lt;/EnforceExtendedAnalyzerRules&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Microsoft.CodeAnalysis.CSharp&#34;</span>
</span></span><span class="line"><span class="cl">                      <span class="na">Version=</span><span class="s">&#34;4.8.0&#34;</span>
</span></span><span class="line"><span class="cl">                      <span class="na">PrivateAssets=</span><span class="s">&#34;all&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Microsoft.CodeAnalysis.Analyzers&#34;</span>
</span></span><span class="line"><span class="cl">                      <span class="na">Version=</span><span class="s">&#34;3.11.0&#34;</span>
</span></span><span class="line"><span class="cl">                      <span class="na">PrivateAssets=</span><span class="s">&#34;all&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/Project&gt;</span>
</span></span></code></pre></div><p>Two details deserve emphasis. <code>LangVersion</code> is decoupled from the TFM — <code>netstandard2.0</code> defaults to C# 7.3, but the language version is a compiler switch, so <code>latest</code> gives you records, pattern matching, and raw string literals while still producing a <code>netstandard2.0</code> assembly. And <code>PrivateAssets=&quot;all&quot;</code> on both Roslyn packages is mandatory: the compiler <em>provides</em> <code>Microsoft.CodeAnalysis</code> at runtime, and if your package declares it as a NuGet dependency, every consumer inherits a dependency on a specific Roslyn version they never asked for. The <code>Microsoft.CodeAnalysis.CSharp</code> version you compile against is also your minimum supported compiler — 4.8.0 means SDK 8.0.100 or newer, so pick it deliberately, not by taking the latest.</p>




<h2 id="the-package-layout-analyzersdotnetcs-or-nothing"><a href="/posts/incremental-source-generators-packaging/#the-package-layout-analyzersdotnetcs-or-nothing" title="The Package Layout: analyzers/dotnet/cs or Nothing">The Package Layout: analyzers/dotnet/cs or Nothing</a></h2>
<p>NuGet decides what your DLL <em>is</em> by where it sits in the package. Assemblies under <code>lib/&lt;tfm&gt;/</code> are compile-time and runtime references. Generators and analyzers live under <code>analyzers/dotnet/cs/</code> — the conventional path for C#-only generators. (NuGet also loads assemblies from the language-agnostic <code>analyzers/dotnet/</code> folder, but a C# generator has no business being offered to VB compilations, so use the <code>cs</code> subfolder.) The SDK&rsquo;s restore targets read that folder to populate the <code>@(Analyzer)</code> item group that gets passed to the compiler.</p>
<p>The default <code>dotnet pack</code> puts your build output into <code>lib/netstandard2.0/</code>, which is precisely wrong twice over. The consumer gets your generator assembly as a <em>runtime dependency</em> — it ships to their <code>bin</code> folder and their deployment, referencing Roslyn types that may not resolve. And the compiler never receives it as an analyzer, so the generator simply never runs. No error. The user&rsquo;s <code>[AutoToString]</code> attribute (from <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2</a>) does nothing, and the first symptom is a compile error about a missing generated member.</p>
<p>The fix is two moves in the csproj: suppress the default placement, then pack the DLL explicitly into the analyzer path.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="nt">&lt;PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="c">&lt;!-- Keep the build output out of lib/. --&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;IncludeBuildOutput&gt;</span>false<span class="nt">&lt;/IncludeBuildOutput&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">  <span class="c">&lt;!-- Suppress NU5128: no lib/ folder is intentional here. --&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;NoWarn&gt;</span>$(NoWarn);NU5128<span class="nt">&lt;/NoWarn&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;None</span> <span class="na">Include=</span><span class="s">&#34;$(OutputPath)\$(AssemblyName).dll&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">Pack=</span><span class="s">&#34;true&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">PackagePath=</span><span class="s">&#34;analyzers/dotnet/cs&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">Visible=</span><span class="s">&#34;false&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/ItemGroup&gt;</span>
</span></span></code></pre></div><p>Run <code>dotnet pack</code> and inspect the result — a <code>.nupkg</code> is a zip archive, so any archive tool shows you the truth:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">AutoToString.Generators.1.0.0.nupkg
</span></span><span class="line"><span class="cl">├── analyzers/
</span></span><span class="line"><span class="cl">│   └── dotnet/
</span></span><span class="line"><span class="cl">│       └── cs/
</span></span><span class="line"><span class="cl">│           └── AutoToString.Generators.dll   ← the compiler loads this
</span></span><span class="line"><span class="cl">├── AutoToString.Generators.nuspec
</span></span><span class="line"><span class="cl">└── [no lib/ folder — on purpose]
</span></span></code></pre></div><p>I verify this layout in CI with a script that unzips the package and asserts the path exists. It is a five-line check that has caught more release-breaking mistakes than any other test in my generator repositories.</p>




<h2 id="dependencies-are-your-problem-not-nugets"><a href="/posts/incremental-source-generators-packaging/#dependencies-are-your-problem-not-nugets" title="Dependencies Are Your Problem, Not NuGet&rsquo;s">Dependencies Are Your Problem, Not NuGet&rsquo;s</a></h2>
<p>Here is the rule that surprises everyone once: <strong>the compiler does not restore your generator&rsquo;s NuGet dependencies.</strong> When Roslyn loads <code>analyzers/dotnet/cs/AutoToString.Generators.dll</code>, it loads the assemblies in that folder — and nothing else. A <code>PackageReference</code> to <code>Newtonsoft.Json</code> in your generator project restores fine on your machine, but in the consumer&rsquo;s build there is no restore step for analyzer dependencies. Your generator throws <code>FileNotFoundException</code> inside the compiler, which surfaces as a cryptic <code>CS8785</code> warning about a generator failing, and the consumer blames you. Correctly.</p>
<p>The mechanical fix: pack the dependency&rsquo;s DLL into the same analyzer folder, and tell Roslyn&rsquo;s project system about it for the in-IDE case. <code>GeneratePathProperty=&quot;true&quot;</code> on the <code>PackageReference</code> gives you an MSBuild property pointing into the restored package (the pattern is <code>$(PKG&lt;PackageId&gt;)</code> with dots replaced by underscores):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Newtonsoft.Json&#34;</span>
</span></span><span class="line"><span class="cl">                    <span class="na">Version=</span><span class="s">&#34;13.0.3&#34;</span>
</span></span><span class="line"><span class="cl">                    <span class="na">PrivateAssets=</span><span class="s">&#34;all&#34;</span>
</span></span><span class="line"><span class="cl">                    <span class="na">GeneratePathProperty=</span><span class="s">&#34;true&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="nt">&lt;PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;GetTargetPathDependsOn&gt;</span>
</span></span><span class="line"><span class="cl">    $(GetTargetPathDependsOn);GetDependencyTargetPaths
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/GetTargetPathDependsOn&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/PropertyGroup&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c">&lt;!-- Makes the dependency visible to the consuming project&#39;s
</span></span></span><span class="line"><span class="cl"><span class="c">     analyzer set when referenced via ProjectReference. --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;Target</span> <span class="na">Name=</span><span class="s">&#34;GetDependencyTargetPaths&#34;</span><span class="nt">&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&lt;TargetPathWithTargetPlatformMoniker</span>
</span></span><span class="line"><span class="cl">        <span class="na">Include=</span><span class="s">&#34;$(PKGNewtonsoft_Json)\lib\netstandard2.0\Newtonsoft.Json.dll&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">IncludeRuntimeDependency=</span><span class="s">&#34;false&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;/ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/Target&gt;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c">&lt;!-- And into the package, next to the generator itself. --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;None</span> <span class="na">Include=</span><span class="s">&#34;$(PKGNewtonsoft_Json)\lib\netstandard2.0\Newtonsoft.Json.dll&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">Pack=</span><span class="s">&#34;true&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">PackagePath=</span><span class="s">&#34;analyzers/dotnet/cs&#34;</span>
</span></span><span class="line"><span class="cl">        <span class="na">Visible=</span><span class="s">&#34;false&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/ItemGroup&gt;</span>
</span></span></code></pre></div><p>This works, and it is also a trap with a bow on it. All analyzers in a consumer&rsquo;s build share a load context per host — if two generator packages each bundle a <em>different</em> version of the same dependency, one of them loses, non-deterministically, depending on load order. This is not theoretical; it is a recurring issue label in half the generator repositories on GitHub.</p>
<p>Which is why my actual advice is blunter: the correct number of dependencies for a source generator is <strong>zero</strong>. You are generating strings from syntax. You do not need a JSON library — <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2&rsquo;s <code>AdditionalTexts</code> pattern</a> can parse a config file with fifty lines of hand-rolled code that never version-conflicts with anyone. Every dependency you bundle is a collision waiting for a consumer whose build you cannot see.</p>




<h2 id="delivering-the-marker-attribute"><a href="/posts/incremental-source-generators-packaging/#delivering-the-marker-attribute" title="Delivering the Marker Attribute">Delivering the Marker Attribute</a></h2>
<p>Your generator triggers on an attribute. Somebody has to define it, and you have exactly two good options.</p>
<p><strong>Option 1: generate it</strong>, via <code>RegisterPostInitializationOutput</code> as shown in <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2</a> — with two refinements for shipping. The attribute must be <code>internal</code>, because if two projects in one solution both use your generator, two <code>public</code> copies of the same attribute type produce ambiguity errors the moment one project references the other. And it should be <code>[Conditional]</code>, so that applications of it vanish from the compiled output:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">private</span> <span class="kd">const</span> <span class="kt">string</span> <span class="n">AttributeSource</span> <span class="p">=</span> <span class="s">&#34;&#34;&#34;
</span></span></span><span class="line"><span class="cl">    <span class="c1">// &lt;auto-generated/&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">namespace</span> <span class="nn">AutoToString</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="cs">/// &lt;summary&gt;Marks a partial class for generation.&lt;/summary&gt;</span>
</span></span><span class="line"><span class="cl"><span class="na">    [System.AttributeUsage(System.AttributeTargets.Class)]</span>
</span></span><span class="line"><span class="cl"><span class="na">    [System.Diagnostics.Conditional(&#34;AUTOTOSTRING_KEEP_ATTRIBUTE&#34;)]</span>
</span></span><span class="line"><span class="cl">    <span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">class</span> <span class="nc">AutoToStringAttribute</span> <span class="p">:</span> <span class="n">System</span><span class="p">.</span><span class="n">Attribute</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="s">&#34;&#34;&#34;;
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">void</span> <span class="n">Initialize</span><span class="p">(</span><span class="n">IncrementalGeneratorInitializationContext</span> <span class="n">context</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">context</span><span class="p">.</span><span class="n">RegisterPostInitializationOutput</span><span class="p">(</span><span class="kd">static</span> <span class="n">ctx</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="n">ctx</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="s">&#34;AutoToStringAttribute.g.cs&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">SourceText</span><span class="p">.</span><span class="n">From</span><span class="p">(</span><span class="n">AttributeSource</span><span class="p">,</span> <span class="n">Encoding</span><span class="p">.</span><span class="n">UTF8</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// ... pipeline as in part 2 ...</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The <code>[Conditional]</code> trick exploits a compiler rule: applications of an attribute whose condition symbol is undefined are <strong>not emitted into the IL</strong> at all. The generator still sees the attribute in syntax and symbols at compile time — <code>ForAttributeWithMetadataName</code> works unchanged — but the consumer&rsquo;s compiled assembly carries no <em>applications</em> of it, so nothing downstream ever takes a dependency on the attribute type. Be precise about what this does not do, though: the attribute&rsquo;s <em>class definition</em> is still compiled into every consuming assembly. Under <code>InternalsVisibleTo</code>, both assemblies define that same internal type and both can see the other&rsquo;s copy, so CS0436 type-conflict warnings fire — <code>[Conditional]</code> does not help here, whatever half the blog posts on the topic claim.</p>
<p>For the <code>InternalsVisibleTo</code> case you have three honest fixes. On Roslyn 4.14 or newer (SDK 9.0.300+), call <code>context.AddEmbeddedAttributeDefinition()</code> and mark your generated attribute with <code>[Embedded]</code> — the compiler then treats the type as invisible outside its own assembly, and the conflict disappears by design; <a href="https://andrewlock.net/exploring-dotnet-10-preview-features-4-solving-the-source-generator-marker-attribute-problem-in-dotnet-10/" target="_blank" rel="noopener external noreferrer">Andrew Lock walks through the mechanism</a>. On older compilers, suppressing CS0436 with <code>NoWarn</code> is the pragmatic fallback — ugly, but harmless, because every copy of the attribute is byte-identical; <a href="https://andrewlock.net/creating-a-source-generator-part-8-solving-the-source-generator-marker-attribute-problem-part2/" target="_blank" rel="noopener external noreferrer">Andrew Lock&rsquo;s earlier deep dive into the marker attribute problem</a> weighs the alternatives. And if neither appeals, option 2 sidesteps generation entirely.</p>
<p><strong>Option 2: a separate runtime package.</strong> If the attribute must survive into IL — because you or your users reflect over it at runtime — generate nothing and ship a tiny ordinary library instead, with the generator package depending on it:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="c">&lt;!-- In AutoToString.Generators.csproj --&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="c">&lt;!-- A real dependency this time: the attributes package is a
</span></span></span><span class="line"><span class="cl"><span class="c">       normal lib/ package that consumers reference at runtime. --&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;AutoToString.Attributes&#34;</span> <span class="na">Version=</span><span class="s">&#34;1.0.0&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/ItemGroup&gt;</span>
</span></span></code></pre></div><p>This is how CommunityToolkit.Mvvm does it: <code>[ObservableProperty]</code> and friends are public types in the runtime assembly, and the generator ships alongside them in the same package. The cost is real — a runtime assembly in every consumer&rsquo;s deployment, and if you split attributes and generator into separate packages, two versions to keep in lockstep. My default is option 1 with <code>[Conditional]</code>; option 2 only when runtime reflection is a genuine, stated requirement — not &ldquo;maybe someday.&rdquo;</p>




<h2 id="debugging-the-shipped-experience"><a href="/posts/incremental-source-generators-packaging/#debugging-the-shipped-experience" title="Debugging the Shipped Experience">Debugging the Shipped Experience</a></h2>
<p>&ldquo;Attach debugger to a compiler process&rdquo; used to be the initiation ritual of generator authors. It is obsolete. The <code>IsRoslynComponent</code> property from the first section unlocks a dedicated debug profile: Visual Studio launches a compilation of a <em>target project</em> with your generator attached and your breakpoints live. The profile goes into <code>Properties/launchSettings.json</code> of the generator project:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-json" data-lang="json"><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&#34;profiles&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="nt">&#34;Debug AutoToString.Generators&#34;</span><span class="p">:</span> <span class="p">{</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;commandName&#34;</span><span class="p">:</span> <span class="s2">&#34;DebugRoslynComponent&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">      <span class="nt">&#34;targetProject&#34;</span><span class="p">:</span> <span class="s2">&#34;../../samples/AutoToString.Sample/AutoToString.Sample.csproj&#34;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">  <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Set the generator project as the startup project, press F5, and you are stepping through your <code>transform</code> against real sample code. This requires the <a href="https://learn.microsoft.com/en-us/dotnet/csharp/roslyn-sdk/" target="_blank" rel="noopener external noreferrer">.NET Compiler Platform SDK component</a> in the Visual Studio installer — a one-time setup cost that repays itself the first time you inspect an <code>IncrementalGeneratorInitializationContext</code> live instead of debugging by <code>AddSource</code>-driven printf.</p>
<p>That covers the generator. It does not cover the <em>package</em> — <code>ProjectReference</code> and <code>PackageReference</code> take different code paths, and only the latter exercises your layout. So before every release, I pack into a local folder and consume the actual <code>.nupkg</code> from a throwaway project:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">dotnet pack src/AutoToString.Generators -o ./local-packages -p:Version=1.0.0-local.1
</span></span><span class="line"><span class="cl">dotnet new console -o /tmp/pkgtest
</span></span><span class="line"><span class="cl">dotnet add /tmp/pkgtest package AutoToString.Generators \
</span></span><span class="line"><span class="cl">    --source &#34;$PWD/local-packages&#34; --prerelease
</span></span><span class="line"><span class="cl">dotnet build /tmp/pkgtest -bl
</span></span></code></pre></div><p>Note the absolute <code>--source</code> path — <code>dotnet add package</code> resolves relative sources against the project you are adding to, not against your shell&rsquo;s working directory, and a silently unresolvable source is exactly the kind of error this smoke test exists to avoid.</p>
<p>Then open the binlog and confirm the generator appears under <code>RunGenerators</code> — the <a href="/posts/dotnet-source-generators-hidden-costs/">same binlog workflow from the predecessor article</a>, pointed at yourself this time. One gotcha: NuGet caches packages by version in the global packages folder, which is why the pack command above stamps a <code>-local.1</code> suffix — bump it to <code>-local.2</code> on the next iteration, or clear <code>~/.nuget/packages/autotostring.generators</code> between attempts. Rebuilding the same version number silently serves you the stale cached copy: twenty minutes of confusion, guaranteed, the first time you forget.</p>




<h2 id="analyzer-hygiene-for-shippers"><a href="/posts/incremental-source-generators-packaging/#analyzer-hygiene-for-shippers" title="Analyzer Hygiene for Shippers">Analyzer Hygiene for Shippers</a></h2>
<p>The <code>Microsoft.CodeAnalysis.Analyzers</code> package you referenced in the first section is not decoration — it is a set of analyzers <em>about</em> analyzers, and two of its rules matter enormously once you ship.</p>
<p><strong>RS1035</strong> fires when analyzer code calls banned APIs: file I/O, <code>Environment.CurrentDirectory</code>, random number generation, anything culture- or machine-dependent. The compiler may run your generator concurrently, repeatedly, and on machines you have never seen; a generator that reads a file from disk produces builds that are non-deterministic by construction. <code>EnforceExtendedAnalyzerRules</code> turns this from a suggestion into an error, which is where it belongs. If you need file content, it enters through <code>AdditionalTexts</code> — that is the supported channel, and the <em>only</em> one.</p>
<p><strong>RS2008</strong> enforces release tracking for your diagnostics. Every <code>DiagnosticDescriptor</code> you declare:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">private</span> <span class="kd">static</span> <span class="k">readonly</span> <span class="n">DiagnosticDescriptor</span> <span class="n">ClassMustBePartial</span> <span class="p">=</span> <span class="k">new</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">id</span><span class="p">:</span> <span class="s">&#34;ATS0001&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">title</span><span class="p">:</span> <span class="s">&#34;Class must be partial&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">messageFormat</span><span class="p">:</span> <span class="s">&#34;The class &#39;{0}&#39; must be declared partial to use [AutoToString]&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">category</span><span class="p">:</span> <span class="s">&#34;Usage&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">defaultSeverity</span><span class="p">:</span> <span class="n">DiagnosticSeverity</span><span class="p">.</span><span class="n">Warning</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">isEnabledByDefault</span><span class="p">:</span> <span class="kc">true</span><span class="p">);</span>
</span></span></code></pre></div><p>must be recorded in two markdown files, <code>AnalyzerReleases.Shipped.md</code> and <code>AnalyzerReleases.Unshipped.md</code>, registered as <code>AdditionalFiles</code> in the project. New rules land in the unshipped file:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">### New Rules
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">Rule ID | Category | Severity | Notes
</span></span><span class="line"><span class="cl">--------|----------|----------|--------------------------------
</span></span><span class="line"><span class="cl">ATS0001 | Usage    | Warning  | Class must be partial
</span></span></code></pre></div><p>On release, the entries move to <code>AnalyzerReleases.Shipped.md</code> under a version heading. This feels like bureaucracy until the first time it stops you from silently changing a diagnostic&rsquo;s severity between minor versions — the analyzer equivalent of a breaking API change, except it breaks builds with <code>TreatWarningsAsErrors</code> instead of breaking compilation. Diagnostic IDs are public API. The tracking files are the changelog that makes that contract auditable.</p>




<h2 id="the-series-closed"><a href="/posts/incremental-source-generators-packaging/#the-series-closed" title="The Series, Closed">The Series, Closed</a></h2>
<p>Four parts, four sentences. Part 1: every value in the pipeline must compare by value — records, <code>EquatableArray&lt;T&gt;</code>, symbols flattened exactly once. Part 2: enter through <code>ForAttributeWithMetadataName</code>, filter early, transform late, project the compilation before combining. Part 3: run the driver twice with step tracking and assert <code>Cached</code> — untested incrementalism is folklore. Part 4: <code>netstandard2.0</code>, <code>analyzers/dotnet/cs</code>, zero dependencies, a <code>[Conditional]</code> attribute, and a <code>.nupkg</code> you actually installed before your users did.</p>
<p>Notice what the packaging part has in common with the other three: none of it is hard, and all of it is invisible until it fails in someone else&rsquo;s build. That is the defining property of shipping a source generator — you are running your code inside other people&rsquo;s compilers, on hosts you do not control, next to generators you have never heard of. The discipline this series describes is not perfectionism. It is the minimum courtesy owed to every developer whose keystroke latency, build time, and dependency graph you are about to join, <a href="/posts/dotnet-source-generators-hidden-costs/">as the predecessor article measured in painful detail</a>.</p>
<p>Correct, incremental, proven, packaged. Now it is done right.</p>
<blockquote>
<p>Your generator runs in everyone else&rsquo;s build. <strong>Package it like you know that.</strong></p>
</blockquote>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/dotnet.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/dotnet.png" medium="image"/>
      <category>dotnet</category>
      <category>csharp</category>
      <category>roslyn</category>
      <category>sourcegenerators</category>
      <category>performance</category>
      <category>bestpractices</category>
      <category>nuget</category>
    </item>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/incremental-source-generators-testing/</guid>
      <link>https://daily-devops.net/posts/incremental-source-generators-testing/</link>
      <title>Incremental Source Generators Done Right: Prove the Cache Hits</title>
      <pubDate>Tue, 28 Jul 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>You followed the equality rules, you shaped the pipeline correctly, and your generator still might be re-running on every keystroke — because nobody ever asked the driver. Roslyn records exactly why each pipeline step re-executed, and you can assert against those reasons in a plain xUnit test. This is the test that turns “my generator is incremental” from a claim into a regression-protected fact, plus the catalog of anti-patterns it catches in the wild.</description>
      <content:encoded><![CDATA[<p>This is part 3 of 4. <a href="/posts/incremental-source-generators-done-right/">Part 1</a> established the equality contract: every value flowing through an <code>IIncrementalGenerator</code> pipeline must compare by value, or the cache misses. <a href="/posts/incremental-source-generators-pipeline-patterns/">Part 2</a> covered the pipeline shapes that keep those comparisons cheap. Both parts end with a claim: <em>do this and your generator caches correctly</em>. Throughout this post, the generator under test is the <code>AutoToStringGenerator</code> we assembled at the end of part 2 — the one that turns <code>[AutoToString]</code> on a partial class into a generated <code>ToString</code> override.</p>
<p>Claims are cheap. I have reviewed generators whose authors swore they were incremental, complete with <code>record</code> models and <code>EquatableArray&lt;T&gt;</code> wrappers, and a single test proved they re-ran the entire pipeline on every unrelated edit. One overlooked tuple, one captured symbol, and the whole thing silently degrades to the <a href="/posts/dotnet-source-generators-hidden-costs/">full-rebuild behavior we measured in the predecessor article</a>.</p>
<p>If you do not write that test, your generator&rsquo;s incrementalism is folklore. This post is the test.</p>




<h2 id="asking-the-driver"><a href="/posts/incremental-source-generators-testing/#asking-the-driver" title="Asking the Driver">Asking the Driver</a></h2>
<p>Roslyn can tell you exactly why each pipeline step ran. The <code>GeneratorDriver</code> records this when you opt in via <code>GeneratorDriverOptions</code> with <code>trackIncrementalGeneratorSteps: true</code>. Tracking is off by default because it costs memory; you enable it only in tests.</p>
<p>The setup is ordinary Roslyn hosting: build a <code>CSharpCompilation</code>, wrap your generator in a driver, run it.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">Microsoft.CodeAnalysis</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">Microsoft.CodeAnalysis.CSharp</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">static</span> <span class="k">class</span> <span class="nc">GeneratorTestHost</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="n">CSharpCompilation</span> <span class="n">CreateCompilation</span><span class="p">(</span><span class="k">params</span> <span class="kt">string</span><span class="p">[]</span> <span class="n">sources</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">syntaxTrees</span> <span class="p">=</span> <span class="n">sources</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="n">source</span> <span class="p">=&gt;</span> <span class="n">CSharpSyntaxTree</span><span class="p">.</span><span class="n">ParseText</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="n">source</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="k">new</span> <span class="n">CSharpParseOptions</span><span class="p">(</span><span class="n">LanguageVersion</span><span class="p">.</span><span class="n">Latest</span><span class="p">)))</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">ToArray</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">CSharpCompilation</span><span class="p">.</span><span class="n">Create</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">            <span class="n">assemblyName</span><span class="p">:</span> <span class="s">&#34;GeneratorTests&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">syntaxTrees</span><span class="p">:</span> <span class="n">syntaxTrees</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">references</span><span class="p">:</span> <span class="n">Basic</span><span class="p">.</span><span class="n">Reference</span><span class="p">.</span><span class="n">Assemblies</span><span class="p">.</span><span class="n">Net90</span><span class="p">.</span><span class="n">References</span><span class="p">.</span><span class="n">All</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">options</span><span class="p">:</span> <span class="k">new</span> <span class="n">CSharpCompilationOptions</span><span class="p">(</span><span class="n">OutputKind</span><span class="p">.</span><span class="n">DynamicallyLinkedLibrary</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="n">GeneratorDriver</span> <span class="n">CreateDriver</span><span class="p">(</span><span class="n">IIncrementalGenerator</span> <span class="n">generator</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="n">CSharpGeneratorDriver</span><span class="p">.</span><span class="n">Create</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">            <span class="n">generators</span><span class="p">:</span> <span class="p">[</span><span class="n">generator</span><span class="p">.</span><span class="n">AsSourceGenerator</span><span class="p">()],</span>
</span></span><span class="line"><span class="cl">            <span class="n">driverOptions</span><span class="p">:</span> <span class="k">new</span> <span class="n">GeneratorDriverOptions</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="n">IncrementalGeneratorOutputKind</span><span class="p">.</span><span class="n">None</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">trackIncrementalGeneratorSteps</span><span class="p">:</span> <span class="kc">true</span><span class="p">));</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Two details matter here. First, the references: <a href="https://github.com/jaredpar/basic-reference-assemblies" target="_blank" rel="noopener external noreferrer"><code>Basic.Reference.Assemblies</code></a> gives you a complete, deterministic reference set without touching the machine&rsquo;s SDK installation. If you refuse the extra package, <code>MetadataReference.CreateFromFile(typeof(object).Assembly.Location)</code> works for trivial cases, but you will chase missing-reference diagnostics the moment your test source uses <code>System.Linq</code>. Take the package.</p>
<p>Second, <code>AsSourceGenerator()</code>: the <code>Create</code> overload that accepts <code>GeneratorDriverOptions</code> takes <code>ISourceGenerator</code> instances, so incremental generators get wrapped. This is the supported adapter, not a hack.</p>
<p>After running, <code>GetRunResult()</code> returns a <code>GeneratorDriverRunResult</code> whose per-generator results expose <code>TrackedSteps</code> — a dictionary keyed by the names you assigned with <code>WithTrackingName</code> in part 2. If you skipped naming your steps, go back and name them. Unnamed steps are invisible to this whole approach.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">GeneratorDriver</span> <span class="n">driver</span> <span class="p">=</span> <span class="n">GeneratorTestHost</span><span class="p">.</span><span class="n">CreateDriver</span><span class="p">(</span><span class="k">new</span> <span class="n">AutoToStringGenerator</span><span class="p">());</span>
</span></span><span class="line"><span class="cl"><span class="n">driver</span> <span class="p">=</span> <span class="n">driver</span><span class="p">.</span><span class="n">RunGenerators</span><span class="p">(</span><span class="n">compilation</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">GeneratorDriverRunResult</span> <span class="n">runResult</span> <span class="p">=</span> <span class="n">driver</span><span class="p">.</span><span class="n">GetRunResult</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">trackedSteps</span> <span class="p">=</span> <span class="n">runResult</span><span class="p">.</span><span class="n">Results</span><span class="p">[</span><span class="m">0</span><span class="p">].</span><span class="n">TrackedSteps</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="c1">// trackedSteps[&#34;Models&#34;], ...</span>
</span></span></code></pre></div><p>Every step you tagged with <code>WithTrackingName</code> shows up in this dictionary. The <code>AutoToStringGenerator</code> names exactly one step, <code>Models</code>, so that is the one key we care about here — a more complex pipeline with named steps after each <code>Combine</code> or <code>Collect</code> gives you more keys, and you assert on all of them.</p>
<p>Note that <code>RunGenerators</code> returns a <em>new</em> driver carrying the cache state. Discard the return value and every subsequent run starts cold — the single most common reason this test passes when it should fail.</p>




<h2 id="the-cache-hit-test"><a href="/posts/incremental-source-generators-testing/#the-cache-hit-test" title="The Cache-Hit Test">The Cache-Hit Test</a></h2>
<p>The test protocol has four steps: run the generator once to populate the cache, apply an <em>unrelated</em> change to the compilation, run again with the same driver instance, and assert that no tracked step reports a cache miss.</p>
<p>Each <code>IncrementalGeneratorRunStep</code> in <code>TrackedSteps</code> exposes <code>Outputs</code>, an array of <code>(object Value, IncrementalStepRunReason Reason)</code> tuples. The reasons are the verdict:</p>
<ul>
<li><code>Cached</code> — the inputs compared equal, the step was skipped entirely. Best case.</li>
<li><code>Unchanged</code> — the step re-executed, but produced an output equal to the cached one, so downstream steps were spared. Acceptable, slightly wasteful.</li>
<li><code>Modified</code> / <code>New</code> — the cache missed. This is what the test forbids.</li>
<li><code>Removed</code> — the input disappeared, so the step re-ran to drop its output. Also a re-run.</li>
</ul>
<p>Here is the complete test, against the <code>AutoToStringGenerator</code> and its <code>Models</code> step:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">Microsoft.CodeAnalysis</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">Microsoft.CodeAnalysis.CSharp</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">Xunit</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">IncrementalityTests</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="kd">const</span> <span class="kt">string</span> <span class="n">Source</span> <span class="p">=</span> <span class="s">&#34;&#34;&#34;
</span></span></span><span class="line"><span class="cl">        <span class="k">using</span> <span class="nn">AutoToString</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">namespace</span> <span class="nn">Demo</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="na">
</span></span></span><span class="line"><span class="cl"><span class="na">        [AutoToString]</span>
</span></span><span class="line"><span class="cl">        <span class="kd">public</span> <span class="kd">partial</span> <span class="k">class</span> <span class="nc">Customer</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="kd">public</span> <span class="kt">string</span> <span class="n">Name</span> <span class="p">{</span> <span class="k">get</span><span class="p">;</span> <span class="k">set</span><span class="p">;</span> <span class="p">}</span> <span class="p">=</span> <span class="s">&#34;&#34;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="kd">public</span> <span class="kt">int</span> <span class="n">Age</span> <span class="p">{</span> <span class="k">get</span><span class="p">;</span> <span class="k">set</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="s">&#34;&#34;&#34;;
</span></span></span><span class="line"><span class="cl"><span class="na">
</span></span></span><span class="line"><span class="cl"><span class="na">    [Fact]</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="k">void</span> <span class="n">UnrelatedChange_DoesNotInvalidateCache</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">compilation</span> <span class="p">=</span> <span class="n">GeneratorTestHost</span><span class="p">.</span><span class="n">CreateCompilation</span><span class="p">(</span><span class="n">Source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">GeneratorDriver</span> <span class="n">driver</span> <span class="p">=</span> <span class="n">GeneratorTestHost</span><span class="p">.</span><span class="n">CreateDriver</span><span class="p">(</span><span class="k">new</span> <span class="n">AutoToStringGenerator</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Run 1: populate the cache.</span>
</span></span><span class="line"><span class="cl">        <span class="n">driver</span> <span class="p">=</span> <span class="n">driver</span><span class="p">.</span><span class="n">RunGenerators</span><span class="p">(</span><span class="n">compilation</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Unrelated change: a new syntax tree the generator does not care about.</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">updatedCompilation</span> <span class="p">=</span> <span class="n">compilation</span><span class="p">.</span><span class="n">AddSyntaxTrees</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">            <span class="n">CSharpSyntaxTree</span><span class="p">.</span><span class="n">ParseText</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="s">&#34;namespace Demo; public class Unrelated { }&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="k">new</span> <span class="n">CSharpParseOptions</span><span class="p">(</span><span class="n">LanguageVersion</span><span class="p">.</span><span class="n">Latest</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Run 2: same driver instance, carrying cache state.</span>
</span></span><span class="line"><span class="cl">        <span class="n">driver</span> <span class="p">=</span> <span class="n">driver</span><span class="p">.</span><span class="n">RunGenerators</span><span class="p">(</span><span class="n">updatedCompilation</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="n">GeneratorDriverRunResult</span> <span class="n">result</span> <span class="p">=</span> <span class="n">driver</span><span class="p">.</span><span class="n">GetRunResult</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="n">AssertAllStepsCached</span><span class="p">(</span><span class="n">result</span><span class="p">,</span> <span class="s">&#34;Models&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="kd">static</span> <span class="k">void</span> <span class="n">AssertAllStepsCached</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">GeneratorDriverRunResult</span> <span class="n">result</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="k">params</span> <span class="kt">string</span><span class="p">[]</span> <span class="n">stepNames</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">trackedSteps</span> <span class="p">=</span> <span class="n">result</span><span class="p">.</span><span class="n">Results</span><span class="p">[</span><span class="m">0</span><span class="p">].</span><span class="n">TrackedSteps</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">foreach</span> <span class="p">(</span><span class="kt">var</span> <span class="n">stepName</span> <span class="k">in</span> <span class="n">stepNames</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">Assert</span><span class="p">.</span><span class="n">True</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="n">trackedSteps</span><span class="p">.</span><span class="n">ContainsKey</span><span class="p">(</span><span class="n">stepName</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">                <span class="s">$&#34;Step &#39;{stepName}&#39; was not tracked. Check WithTrackingName.&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="k">foreach</span> <span class="p">(</span><span class="kt">var</span> <span class="n">step</span> <span class="k">in</span> <span class="n">trackedSteps</span><span class="p">[</span><span class="n">stepName</span><span class="p">])</span>
</span></span><span class="line"><span class="cl">            <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="k">foreach</span> <span class="p">(</span><span class="kt">var</span> <span class="p">(</span><span class="n">_</span><span class="p">,</span> <span class="n">reason</span><span class="p">)</span> <span class="k">in</span> <span class="n">step</span><span class="p">.</span><span class="n">Outputs</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                <span class="p">{</span>
</span></span><span class="line"><span class="cl">                    <span class="n">Assert</span><span class="p">.</span><span class="n">True</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                        <span class="n">reason</span> <span class="k">is</span> <span class="n">IncrementalStepRunReason</span><span class="p">.</span><span class="n">Cached</span>
</span></span><span class="line"><span class="cl">                               <span class="n">or</span> <span class="n">IncrementalStepRunReason</span><span class="p">.</span><span class="n">Unchanged</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                        <span class="s">$&#34;Step &#39;{stepName}&#39; reported &#39;{reason}&#39;. Cache miss.&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">                <span class="p">}</span>
</span></span><span class="line"><span class="cl">            <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Adding an independent syntax tree is the sharpest unrelated change, because it modifies the <code>Compilation</code> — which invalidates anything foolishly bound to <code>CompilationProvider</code> — without touching any syntax your <code>ForAttributeWithMetadataName</code> predicate matches. A whitespace edit inside an existing file is a good second variant; run both.</p>
<p>This test is cheap to run and brutal to pass. That is exactly the point.</p>




<h2 id="the-test-that-catches-the-immutablearray-bug"><a href="/posts/incremental-source-generators-testing/#the-test-that-catches-the-immutablearray-bug" title="The Test That Catches the ImmutableArray Bug">The Test That Catches the ImmutableArray Bug</a></h2>
<p>Part 1 spent an entire section on why <code>ImmutableArray&lt;T&gt;</code> breaks caching: it compares by reference, so two arrays with identical contents are not equal. Here is that bug meeting its test.</p>
<p>Suppose the <code>ClassModel</code> from part 2 had been written the obvious way:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Compiles. Looks incremental. Is not.</span>
</span></span><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string?</span> <span class="n">Namespace</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span></code></pre></div><p>Run the test above against a generator producing this model, and the second run fails:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-text" data-lang="text"><span class="line"><span class="cl">IncrementalityTests.UnrelatedChange_DoesNotInvalidateCache [FAIL]
</span></span><span class="line"><span class="cl">  Assert.True() Failure
</span></span><span class="line"><span class="cl">  Step &#39;Models&#39; reported &#39;Modified&#39;. Cache miss.
</span></span><span class="line"><span class="cl">  Expected: True
</span></span><span class="line"><span class="cl">  Actual:   False
</span></span></code></pre></div><p>The transform re-ran (the compilation changed, so <code>ForAttributeWithMetadataName</code> re-evaluates its matches), produced a <code>ClassModel</code> with contents identical to the cached one — and the record&rsquo;s synthesized equality called <code>ImmutableArray&lt;string&gt;.Equals</code>, which compared references of two distinct arrays and said <em>not equal</em>. Reason: <code>Modified</code>. Everything downstream re-ran too.</p>
<p>The fix is the <code>EquatableArray&lt;T&gt;</code> wrapper from part 1 — the model exactly as part 2 ships it:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string?</span> <span class="n">Namespace</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span></code></pre></div><p>Same test, green. No benchmark, no binlog archaeology, no profiler session — a one-line model change validated by a unit test in milliseconds. This is why the test comes first and the patterns second: the patterns are only trustworthy because the test enforces them.</p>




<h2 id="the-anti-pattern-catalog"><a href="/posts/incremental-source-generators-testing/#the-anti-pattern-catalog" title="The Anti-Pattern Catalog">The Anti-Pattern Catalog</a></h2>
<p>Every entry below is a real failure mode I have hit or reviewed. Each one passes code review and fails the cache-hit test.</p>
<p><strong>1. Holding <code>Compilation</code> or <code>INamedTypeSymbol</code> in the model.</strong> Symbols are reference-equal and tied to a specific compilation; every edit produces a new compilation and new symbols.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: symbol identity changes on every edit.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">Target</span><span class="p">(</span><span class="n">INamedTypeSymbol</span> <span class="n">Type</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: flatten to strings in the transform, drop the symbol.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">Target</span><span class="p">(</span><span class="kt">string</span> <span class="n">FullyQualifiedName</span><span class="p">,</span> <span class="kt">string</span> <span class="n">Namespace</span><span class="p">);</span>
</span></span></code></pre></div><p><strong>2. Anonymous types and tuples carrying arrays out of <code>Select</code>.</strong> Tuples compare element-wise, which is fine for strings — and silently wrong the moment one element is an array.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: the string[] element compares by reference.</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">models</span> <span class="p">=</span> <span class="n">targets</span><span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="p">(</span><span class="n">t</span><span class="p">.</span><span class="n">TypeName</span><span class="p">,</span> <span class="n">Members</span><span class="p">:</span> <span class="n">t</span><span class="p">.</span><span class="n">Members</span><span class="p">.</span><span class="n">ToArray</span><span class="p">()));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: a named record with an equatable collection.</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">models</span> <span class="p">=</span> <span class="n">targets</span><span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="p">(</span><span class="n">t</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">new</span> <span class="n">Target</span><span class="p">(</span><span class="n">t</span><span class="p">.</span><span class="n">TypeName</span><span class="p">,</span> <span class="k">new</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;(</span><span class="n">t</span><span class="p">.</span><span class="n">Members</span><span class="p">)));</span>
</span></span></code></pre></div><p><strong>3. Building the source string inside the transform.</strong> The transform runs on every keystroke that re-evaluates the provider; <code>RegisterSourceOutput</code> runs only on cache misses. Generate text in the latter.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: allocates and formats megabytes of source per keystroke.</span>
</span></span><span class="line"><span class="cl"><span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">SourceEmitter</span><span class="p">.</span><span class="n">Emit</span><span class="p">(</span><span class="n">ExtractModel</span><span class="p">(</span><span class="n">ctx</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: transform extracts the model; emission happens after the cache.</span>
</span></span><span class="line"><span class="cl"><span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">ExtractModel</span><span class="p">(</span><span class="n">ctx</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// ...</span>
</span></span><span class="line"><span class="cl"><span class="n">context</span><span class="p">.</span><span class="n">RegisterSourceOutput</span><span class="p">(</span><span class="n">models</span><span class="p">,</span> <span class="kd">static</span> <span class="p">(</span><span class="n">spc</span><span class="p">,</span> <span class="n">model</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="n">spc</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="s">$&#34;{model.TypeName}.g.cs&#34;</span><span class="p">,</span> <span class="n">SourceEmitter</span><span class="p">.</span><span class="n">Emit</span><span class="p">(</span><span class="n">model</span><span class="p">)));</span>
</span></span></code></pre></div><p><strong>4. Capturing a <code>SyntaxNode</code> in the model and re-reading it later.</strong> Syntax nodes are reference-equal and pin the entire tree in memory — a documented way to leak whole compilations inside the IDE.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: node identity changes on every edit; the tree cannot be collected.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">Target</span><span class="p">(</span><span class="n">ClassDeclarationSyntax</span> <span class="n">Declaration</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: extract what you need, flatten the location for diagnostics.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">Target</span><span class="p">(</span><span class="kt">string</span> <span class="n">TypeName</span><span class="p">,</span> <span class="n">LocationInfo</span><span class="p">?</span> <span class="n">DiagnosticLocation</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">LocationInfo</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">FilePath</span><span class="p">,</span> <span class="n">TextSpan</span> <span class="n">TextSpan</span><span class="p">,</span> <span class="n">LinePositionSpan</span> <span class="n">LineSpan</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">Location</span> <span class="n">ToLocation</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="n">Location</span><span class="p">.</span><span class="n">Create</span><span class="p">(</span><span class="n">FilePath</span><span class="p">,</span> <span class="n">TextSpan</span><span class="p">,</span> <span class="n">LineSpan</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="n">LocationInfo</span> <span class="n">From</span><span class="p">(</span><span class="n">SyntaxNode</span> <span class="n">node</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="k">new</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">            <span class="n">node</span><span class="p">.</span><span class="n">SyntaxTree</span><span class="p">.</span><span class="n">FilePath</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">node</span><span class="p">.</span><span class="n">Span</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">            <span class="n">node</span><span class="p">.</span><span class="n">GetLocation</span><span class="p">().</span><span class="n">GetLineSpan</span><span class="p">().</span><span class="n">Span</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Do not reach for <code>Location</code> as the fix — it carries the same disease. A <code>Location</code> holds a reference to its <code>SyntaxTree</code> and compares that tree by reference, so storing one pins the tree in memory <em>and</em> breaks equality after every edit to the file. Flatten it to file path and spans in the transform, and rebuild the real <code>Location</code> with <code>Location.Create</code> only when you report the diagnostic in <code>RegisterSourceOutput</code>.</p>
<p><strong>5. Non-static lambdas capturing generator state.</strong> A capturing lambda closes over <code>this</code> or a local; the captured state is invisible to the cache and a correctness bug waiting for a second compilation. Mark every pipeline lambda <code>static</code> and the compiler enforces it.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: captures the field; state leaks across runs.</span>
</span></span><span class="line"><span class="cl"><span class="c1">// Inside the generator class:</span>
</span></span><span class="line"><span class="cl"><span class="kd">private</span> <span class="k">readonly</span> <span class="n">List</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">_seen</span> <span class="p">=</span> <span class="p">[];</span>
</span></span><span class="line"><span class="cl"><span class="c1">// Inside Initialize:</span>
</span></span><span class="line"><span class="cl"><span class="n">transform</span><span class="p">:</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="p">{</span> <span class="n">_seen</span><span class="p">.</span><span class="n">Add</span><span class="p">(</span><span class="n">ctx</span><span class="p">.</span><span class="n">TargetSymbol</span><span class="p">.</span><span class="n">Name</span><span class="p">);</span> <span class="k">return</span> <span class="n">Extract</span><span class="p">(</span><span class="n">ctx</span><span class="p">);</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: static lambda, no capture possible.</span>
</span></span><span class="line"><span class="cl"><span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">Extract</span><span class="p">(</span><span class="n">ctx</span><span class="p">)</span>
</span></span></code></pre></div><p><strong>6. Combining <code>CompilationProvider</code> without projecting first.</strong> Covered in depth in <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2</a>: <code>Combine</code> with the raw compilation makes every edit a cache miss for the combined step, because the compilation is never equal to its predecessor.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: invalidated on every keystroke.</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">combined</span> <span class="p">=</span> <span class="n">targets</span><span class="p">.</span><span class="n">Combine</span><span class="p">(</span><span class="n">context</span><span class="p">.</span><span class="n">CompilationProvider</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: project to the stable value you actually need.</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">assemblyName</span> <span class="p">=</span> <span class="n">context</span><span class="p">.</span><span class="n">CompilationProvider</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="p">(</span><span class="n">c</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">c</span><span class="p">.</span><span class="n">AssemblyName</span> <span class="p">??</span> <span class="kt">string</span><span class="p">.</span><span class="n">Empty</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">combined</span> <span class="p">=</span> <span class="n">targets</span><span class="p">.</span><span class="n">Combine</span><span class="p">(</span><span class="n">assemblyName</span><span class="p">);</span>
</span></span></code></pre></div><p>Six patterns, one test. You do not need to memorize the catalog if the test is in CI — it will find the seventh pattern I have not hit yet, too.</p>




<h2 id="wire-it-into-ci"><a href="/posts/incremental-source-generators-testing/#wire-it-into-ci" title="Wire It into CI">Wire It into CI</a></h2>
<p>The incrementality test is a plain xUnit fact; it needs no special runner, no MSBuild integration, no IDE. It runs in the generator&rsquo;s test project alongside everything else. The project file needs the Roslyn C# compiler package and the reference assemblies:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-xml" data-lang="xml"><span class="line"><span class="cl"><span class="nt">&lt;ItemGroup&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Microsoft.CodeAnalysis.CSharp&#34;</span> <span class="na">Version=</span><span class="s">&#34;4.14.0&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Basic.Reference.Assemblies&#34;</span> <span class="na">Version=</span><span class="s">&#34;1.8.0&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Verify.SourceGenerators&#34;</span> <span class="na">Version=</span><span class="s">&#34;2.5.0&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;PackageReference</span> <span class="na">Include=</span><span class="s">&#34;Verify.Xunit&#34;</span> <span class="na">Version=</span><span class="s">&#34;30.4.0&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl">  <span class="nt">&lt;ProjectReference</span> <span class="na">Include=</span><span class="s">&#34;..\..\src\AutoToString.Generators\AutoToString.Generators.csproj&#34;</span> <span class="nt">/&gt;</span>
</span></span><span class="line"><span class="cl"><span class="nt">&lt;/ItemGroup&gt;</span>
</span></span></code></pre></div><p>The cache-hit test proves the generator runs <em>rarely</em>. It says nothing about whether the generated code is <em>correct</em> — that is the job of snapshot testing, and <a href="https://github.com/VerifyTests/Verify.SourceGenerators" target="_blank" rel="noopener external noreferrer">Verify.SourceGenerators</a> does it with almost no ceremony. The one piece of ceremony it does demand is a module initializer that registers its converters — without it, <code>Verify</code> throws instead of producing a snapshot:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">static</span> <span class="k">class</span> <span class="nc">VerifyInit</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl"><span class="na">    [ModuleInitializer]</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="k">void</span> <span class="n">Init</span><span class="p">()</span> <span class="p">=&gt;</span> <span class="n">VerifySourceGenerators</span><span class="p">.</span><span class="n">Initialize</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="na">
</span></span></span><span class="line"><span class="cl"><span class="na">[Fact]</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="n">Task</span> <span class="n">GeneratedSource_MatchesSnapshot</span><span class="p">()</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="n">compilation</span> <span class="p">=</span> <span class="n">GeneratorTestHost</span><span class="p">.</span><span class="n">CreateCompilation</span><span class="p">(</span><span class="n">Source</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">GeneratorDriver</span> <span class="n">driver</span> <span class="p">=</span> <span class="n">GeneratorTestHost</span><span class="p">.</span><span class="n">CreateDriver</span><span class="p">(</span><span class="k">new</span> <span class="n">AutoToStringGenerator</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">    <span class="n">driver</span> <span class="p">=</span> <span class="n">driver</span><span class="p">.</span><span class="n">RunGenerators</span><span class="p">(</span><span class="n">compilation</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="n">Verify</span><span class="p">(</span><span class="n">driver</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Verify serializes every generated hint name, source text, and diagnostic into <code>.verified.cs</code> files committed to the repository. A change in output shows up as a reviewable diff in the pull request instead of a surprise in a consumer&rsquo;s build. The two tests are complementary and both belong in the default <code>dotnet test</code> run: one guards performance, the other guards output.</p>




<h2 id="three-parts-in-three-sentences"><a href="/posts/incremental-source-generators-testing/#three-parts-in-three-sentences" title="Three Parts in Three Sentences">Three Parts in Three Sentences</a></h2>
<p>Part 1: every value in the pipeline must implement value equality, and <code>ImmutableArray&lt;T&gt;</code> does not — flatten symbols to records and wrap collections in <code>EquatableArray&lt;T&gt;</code>. Part 2: filter early with <code>ForAttributeWithMetadataName</code>, transform late, project <code>CompilationProvider</code> before combining, and name every step with <code>WithTrackingName</code>. Part 3: none of that counts until a test runs the driver twice with <code>trackIncrementalGeneratorSteps: true</code> and asserts every tracked step reports <code>Cached</code> or <code>Unchanged</code>.</p>
<p>Wired right, a non-trivial generator on a 200-file project completes most keystrokes in single-digit milliseconds, and the <a href="/posts/dotnet-source-generators-hidden-costs/">multi-second build tax from the predecessor article</a> simply does not apply to you. Wired wrong, you are shipping that tax to every consumer, on every target framework, forever — and telling yourself it is fine because the interface name says <code>Incremental</code>.</p>
<p>The difference is not magic and it is not tribal knowledge. It is one equality contract, one pipeline shape, and one test that refuses to take your word for it. And once all three hold, one question remains: whether the generator survives being packaged and shipped into projects you will never see.</p>
<blockquote>
<p>The interface makes it possible. <strong>The test makes it true.</strong></p>
</blockquote>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/dotnet.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/dotnet.png" medium="image"/>
      <category>dotnet</category>
      <category>csharp</category>
      <category>roslyn</category>
      <category>sourcegenerators</category>
      <category>performance</category>
      <category>bestpractices</category>
    </item>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/incremental-source-generators-pipeline-patterns/</guid>
      <link>https://daily-devops.net/posts/incremental-source-generators-pipeline-patterns/</link>
      <title>Incremental Source Generators Done Right: Pipeline Patterns That Scale</title>
      <pubDate>Thu, 23 Jul 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>Part 1 of this series established the equality contract: every value flowing through an incremental generator pipeline must be comparable by value, or the cache misses and the generator re-runs on every keystroke. This part is about the pipeline itself — ForAttributeWithMetadataName as the entry point, where to filter, where to transform, how to combine providers without accidentally subscribing to the entire Compilation, and the Collect trap that silently breaks everything you fixed in part 1. It ends with a complete worked generator you can steal.</description>
      <content:encoded><![CDATA[<p>This is part 2 of 4 in the Incremental Source Generators Done Right series. <a href="/posts/incremental-source-generators-done-right/">Part 1</a> established the equality contract: every value in the pipeline is cached and compared, and if your model type does not implement value equality, your generator re-runs on every keystroke no matter how clever the rest of your code is. If you have not read it, start there — nothing in this post works without it.</p>
<p>This part is about the pipeline itself. You can have perfectly equatable models and still ship a generator that hammers the IDE, because the <em>shape</em> of the pipeline decides how much work happens before the cache even gets a chance to help. And as <a href="/posts/dotnet-source-generators-hidden-costs/">the predecessor article on hidden generator costs</a> showed, that work is multiplied by every build, every target framework, and every keystroke a developer types for the lifetime of the project.</p>
<p>The rules are few. The ways to violate them are many. Let us walk through them.</p>




<h2 id="forattributewithmetadataname-the-modern-entry-point"><a href="/posts/incremental-source-generators-pipeline-patterns/#forattributewithmetadataname-the-modern-entry-point" title="ForAttributeWithMetadataName: The Modern Entry Point">ForAttributeWithMetadataName: The Modern Entry Point</a></h2>
<p>Almost every generator I have written or reviewed follows the same trigger pattern: the user decorates a type or member with a marker attribute, and the generator produces code for it. Roslyn has a dedicated, heavily optimized API for exactly this pattern: <code>SyntaxProvider.ForAttributeWithMetadataName</code>, available since Microsoft.CodeAnalysis 4.3.</p>
<p>First, the marker attribute itself. Do not ask users to define it — deliver it from the generator via <code>RegisterPostInitializationOutput</code>, which runs exactly once per generator instance, before any user code is processed:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="na">[Generator]</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">class</span> <span class="nc">AutoToStringGenerator</span> <span class="p">:</span> <span class="n">IIncrementalGenerator</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="kd">const</span> <span class="kt">string</span> <span class="n">AttributeSource</span> <span class="p">=</span> <span class="s">&#34;&#34;&#34;
</span></span></span><span class="line"><span class="cl">        <span class="c1">// &lt;auto-generated/&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="k">namespace</span> <span class="nn">AutoToString</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="na">
</span></span></span><span class="line"><span class="cl"><span class="na">        [System.AttributeUsage(System.AttributeTargets.Class)]</span>
</span></span><span class="line"><span class="cl">        <span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">class</span> <span class="nc">AutoToStringAttribute</span> <span class="p">:</span> <span class="n">System</span><span class="p">.</span><span class="n">Attribute</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">        <span class="s">&#34;&#34;&#34;;
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="k">void</span> <span class="n">Initialize</span><span class="p">(</span><span class="n">IncrementalGeneratorInitializationContext</span> <span class="n">context</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">context</span><span class="p">.</span><span class="n">RegisterPostInitializationOutput</span><span class="p">(</span><span class="kd">static</span> <span class="n">ctx</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">            <span class="n">ctx</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="s">&#34;AutoToStringAttribute.g.cs&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">SourceText</span><span class="p">.</span><span class="n">From</span><span class="p">(</span><span class="n">AttributeSource</span><span class="p">,</span> <span class="n">Encoding</span><span class="p">.</span><span class="n">UTF8</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Then the pipeline entry point, with the predicate and transform both <code>static</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">IncrementalValuesProvider</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">?&gt;</span> <span class="n">classes</span> <span class="p">=</span> <span class="n">context</span><span class="p">.</span><span class="n">SyntaxProvider</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">ForAttributeWithMetadataName</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">fullyQualifiedMetadataName</span><span class="p">:</span> <span class="s">&#34;AutoToString.AutoToStringAttribute&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">predicate</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">node</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">node</span> <span class="k">is</span> <span class="n">ClassDeclarationSyntax</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">ct</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">ClassModel</span><span class="p">.</span><span class="n">Create</span><span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">ct</span><span class="p">));</span>
</span></span></code></pre></div><p>Why not the older <code>CreateSyntaxProvider</code>? Because <code>CreateSyntaxProvider</code> calls your predicate for every syntax node — the driver caches per syntax tree, so that means every node in the project on every host start, and every node in the edited file on every keystroke. <code>ForAttributeWithMetadataName</code> instead uses Roslyn&rsquo;s internal attribute index: it pre-filters to nodes that syntactically carry an attribute whose name could match, then verifies the full metadata name semantically. Published benchmarks put this at roughly 99% fewer nodes visited compared to a naive syntax provider. Same result, two orders of magnitude less work — and that work sits on the typing hot path in the IDE, so the difference is directly perceptible.</p>
<p>The <code>static</code> keyword on both lambdas is not decoration. A lambda that captures state drags that state into the cached pipeline node, and captured state is almost never value-equatable — a captured <code>Compilation</code>, a captured list, a captured <code>this</code>. <code>static</code> makes the compiler reject captures outright. Write every pipeline lambda as <code>static</code> and let the compiler enforce what code review would otherwise miss.</p>




<h2 id="filter-early-transform-late"><a href="/posts/incremental-source-generators-pipeline-patterns/#filter-early-transform-late" title="Filter Early, Transform Late">Filter Early, Transform Late</a></h2>
<p>Every operator boundary in the pipeline — every <code>.Select</code>, <code>.Where</code>, <code>.Combine</code> — is a cache node. The framework memoizes the output of each node and only re-executes downstream nodes when the input actually changed (by value equality, see <a href="/posts/incremental-source-generators-done-right/">part 1</a>). This gives you a simple layering rule: do the cheapest checks earliest, and defer the most expensive work to the latest possible node.</p>
<p>The predicate should be syntax-only and dumb. No semantic model, no symbol lookups, just shape checks:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">predicate</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">node</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="n">node</span> <span class="k">is</span> <span class="n">ClassDeclarationSyntax</span> <span class="p">{</span> <span class="n">Members</span><span class="p">.</span><span class="n">Count</span><span class="p">:</span> <span class="p">&gt;</span> <span class="m">0</span> <span class="p">}</span> <span class="n">cls</span>
</span></span><span class="line"><span class="cl">    <span class="p">&amp;&amp;</span> <span class="n">cls</span><span class="p">.</span><span class="n">Modifiers</span><span class="p">.</span><span class="n">Any</span><span class="p">(</span><span class="n">SyntaxKind</span><span class="p">.</span><span class="n">PartialKeyword</span><span class="p">),</span>
</span></span></code></pre></div><p>The transform is where you touch the semantic model — once — and immediately flatten everything into your equatable record. Extract the minimum you need to generate code, and nothing else. Return <code>null</code> for cases you cannot handle, and filter them in a separate <code>Where</code> node:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">IncrementalValuesProvider</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">&gt;</span> <span class="n">models</span> <span class="p">=</span> <span class="n">context</span><span class="p">.</span><span class="n">SyntaxProvider</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">ForAttributeWithMetadataName</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="s">&#34;AutoToString.AutoToStringAttribute&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">predicate</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">node</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">node</span> <span class="k">is</span> <span class="n">ClassDeclarationSyntax</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">ct</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(</span><span class="n">ctx</span><span class="p">.</span><span class="n">TargetSymbol</span> <span class="k">is</span> <span class="n">not</span> <span class="n">INamedTypeSymbol</span> <span class="n">symbol</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="k">return</span> <span class="kc">null</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="c1">// Flatten to primitives immediately. The symbol must NOT</span>
</span></span><span class="line"><span class="cl">            <span class="c1">// survive past this method — it is reference-equal only</span>
</span></span><span class="line"><span class="cl">            <span class="c1">// and roots the entire compilation in memory.</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="k">new</span> <span class="n">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="n">Namespace</span><span class="p">:</span> <span class="n">symbol</span><span class="p">.</span><span class="n">ContainingNamespace</span><span class="p">.</span><span class="n">ToDisplayString</span><span class="p">(),</span>
</span></span><span class="line"><span class="cl">                <span class="n">Name</span><span class="p">:</span> <span class="n">symbol</span><span class="p">.</span><span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">Properties</span><span class="p">:</span> <span class="k">new</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;(</span>
</span></span><span class="line"><span class="cl">                    <span class="n">symbol</span><span class="p">.</span><span class="n">GetMembers</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">                        <span class="p">.</span><span class="n">OfType</span><span class="p">&lt;</span><span class="n">IPropertySymbol</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">                        <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">DeclaredAccessibility</span> <span class="p">==</span> <span class="n">Accessibility</span><span class="p">.</span><span class="n">Public</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                        <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">Name</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                        <span class="p">.</span><span class="n">ToImmutableArray</span><span class="p">()));</span>
</span></span><span class="line"><span class="cl">        <span class="p">})</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="kd">static</span> <span class="n">model</span> <span class="p">=&gt;</span> <span class="n">model</span> <span class="k">is</span> <span class="n">not</span> <span class="kc">null</span><span class="p">)!;</span>
</span></span></code></pre></div><p>And the most common misplacement of all: string building. The generated source text does not belong in the transform. Building it there means you re-allocate the full output string on every re-run of that node, and worse, the (potentially large) string becomes the cached value that gets compared. Source text belongs in <code>RegisterSourceOutput</code>, the terminal node — it only executes when the model actually changed, which is precisely when you <em>want</em> to rebuild the string:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">context</span><span class="p">.</span><span class="n">RegisterSourceOutput</span><span class="p">(</span><span class="n">models</span><span class="p">,</span> <span class="kd">static</span> <span class="p">(</span><span class="n">spc</span><span class="p">,</span> <span class="n">model</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Correct place for string building: this only runs on real changes.</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">source</span> <span class="p">=</span> <span class="n">ToStringEmitter</span><span class="p">.</span><span class="n">Emit</span><span class="p">(</span><span class="n">model</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">spc</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="s">$&#34;{model.Name}.AutoToString.g.cs&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">SourceText</span><span class="p">.</span><span class="n">From</span><span class="p">(</span><span class="n">source</span><span class="p">,</span> <span class="n">Encoding</span><span class="p">.</span><span class="n">UTF8</span><span class="p">));</span>
</span></span><span class="line"><span class="cl"><span class="p">});</span>
</span></span></code></pre></div><p>Predicate filters syntax. Transform extracts data. Output builds strings. Three layers, three costs, in ascending order — and the cache sits between each of them.</p>




<h2 id="combining-providers-without-killing-the-cache"><a href="/posts/incremental-source-generators-pipeline-patterns/#combining-providers-without-killing-the-cache" title="Combining Providers Without Killing the Cache">Combining Providers Without Killing the Cache</a></h2>
<p>Sooner or later your generator needs information beyond the annotated node — the assembly name, the language version, an MSBuild property. That is what <code>Combine</code> is for, and it is where most real-world generators quietly destroy their incrementality.</p>
<p>Here is the classic mistake:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// BROKEN: do not do this.</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">withCompilation</span> <span class="p">=</span> <span class="n">models</span><span class="p">.</span><span class="n">Combine</span><span class="p">(</span><span class="n">context</span><span class="p">.</span><span class="n">CompilationProvider</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">context</span><span class="p">.</span><span class="n">RegisterSourceOutput</span><span class="p">(</span><span class="n">withCompilation</span><span class="p">,</span> <span class="kd">static</span> <span class="p">(</span><span class="n">spc</span><span class="p">,</span> <span class="n">pair</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="p">(</span><span class="n">model</span><span class="p">,</span> <span class="n">compilation</span><span class="p">)</span> <span class="p">=</span> <span class="n">pair</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">asm</span> <span class="p">=</span> <span class="n">compilation</span><span class="p">.</span><span class="n">AssemblyName</span> <span class="p">??</span> <span class="s">&#34;Unknown&#34;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// ... generate using asm ...</span>
</span></span><span class="line"><span class="cl"><span class="p">});</span>
</span></span></code></pre></div><p>It compiles. It works. And it re-runs the source output for <strong>every model on every keystroke</strong>, because a <code>Compilation</code> is a new instance after every edit — that is how Roslyn&rsquo;s immutable model works. Combining with <code>context.CompilationProvider</code> directly means your terminal node&rsquo;s input changes whenever <em>anything anywhere in the project</em> changes. You have re-implemented <code>ISourceGenerator</code> with extra steps.</p>
<p>The fix is mechanical: <code>Select</code> the specific values you need <em>out</em> of the compilation <strong>before</strong> the <code>Combine</code>, so the intermediate node produces a small equatable value that only changes when those values change:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// FIXED: extract only what you need, then combine.</span>
</span></span><span class="line"><span class="cl"><span class="n">IncrementalValueProvider</span><span class="p">&lt;(</span><span class="kt">string</span> <span class="n">AssemblyName</span><span class="p">,</span> <span class="kt">bool</span> <span class="n">SupportsRawStrings</span><span class="p">)&gt;</span> <span class="n">settings</span> <span class="p">=</span>
</span></span><span class="line"><span class="cl">    <span class="n">context</span><span class="p">.</span><span class="n">CompilationProvider</span><span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="p">(</span><span class="n">compilation</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">AssemblyName</span><span class="p">:</span> <span class="n">compilation</span><span class="p">.</span><span class="n">AssemblyName</span> <span class="p">??</span> <span class="s">&#34;Unknown&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">SupportsRawStrings</span><span class="p">:</span> <span class="n">compilation</span> <span class="k">is</span> <span class="n">CSharpCompilation</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">LanguageVersion</span><span class="p">:</span> <span class="p">&gt;=</span> <span class="n">LanguageVersion</span><span class="p">.</span><span class="n">CSharp11</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">withSettings</span> <span class="p">=</span> <span class="n">models</span><span class="p">.</span><span class="n">Combine</span><span class="p">(</span><span class="n">settings</span><span class="p">);</span>
</span></span></code></pre></div><p>The <code>Select</code> node still executes on every edit — that is unavoidable, its input is the compilation. But it is trivially cheap, and its <em>output</em> is a value tuple of a string and a bool, which compares by value. As long as the assembly name and language version do not change (they do not, between keystrokes), everything downstream of the <code>Combine</code> stays cached. One <code>Select</code> in the right place is the difference between a generator that idles and one that burns a core while you type.</p>
<p>The same pattern applies to <code>context.AnalyzerConfigOptionsProvider</code> when you read MSBuild properties: select the property values out first, combine second.</p>




<h2 id="additionaltexts-done-right"><a href="/posts/incremental-source-generators-pipeline-patterns/#additionaltexts-done-right" title="AdditionalTexts Done Right">AdditionalTexts Done Right</a></h2>
<p>Some generators are driven by files rather than attributes — a JSON manifest, a <code>.resx</code>, a config file. Those arrive via <code>context.AdditionalTextsProvider</code>, and the same layering rules apply: filter on the cheap property (the path), then transform the content into an equatable model.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">IncrementalValuesProvider</span><span class="p">&lt;</span><span class="n">EndpointManifest</span><span class="p">?&gt;</span> <span class="n">manifests</span> <span class="p">=</span> <span class="n">context</span><span class="p">.</span><span class="n">AdditionalTextsProvider</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="kd">static</span> <span class="n">file</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="n">file</span><span class="p">.</span><span class="n">Path</span><span class="p">.</span><span class="n">EndsWith</span><span class="p">(</span><span class="s">&#34;endpoints.json&#34;</span><span class="p">,</span> <span class="n">StringComparison</span><span class="p">.</span><span class="n">OrdinalIgnoreCase</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="p">(</span><span class="n">file</span><span class="p">,</span> <span class="n">ct</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">string?</span> <span class="n">content</span> <span class="p">=</span> <span class="n">file</span><span class="p">.</span><span class="n">GetText</span><span class="p">(</span><span class="n">ct</span><span class="p">)?.</span><span class="n">ToString</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">content</span> <span class="k">is</span> <span class="kc">null</span> <span class="p">?</span> <span class="kc">null</span> <span class="p">:</span> <span class="n">EndpointManifest</span><span class="p">.</span><span class="n">Parse</span><span class="p">(</span><span class="n">content</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">})</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="kd">static</span> <span class="n">manifest</span> <span class="p">=&gt;</span> <span class="n">manifest</span> <span class="k">is</span> <span class="n">not</span> <span class="kc">null</span><span class="p">);</span>
</span></span></code></pre></div><p>Two details matter here. First, always pass the <code>CancellationToken</code> to <code>GetText</code> — the IDE cancels stale pipeline runs constantly, and a transform that ignores cancellation makes typing latency worse for no benefit. Second, be honest about what is and is not cached. The driver compares <code>AdditionalText</code> instances, and a touched file produces a new instance — so the <code>Select</code> re-runs, <code>GetText</code> and <code>Parse</code> execute again. What protects everything downstream is the <em>output</em> of the <code>Select</code>: if <code>EndpointManifest.Parse</code> returns an equatable model and the content did not actually change, the model compares equal to the cached one and nothing after this node re-runs. The caching boundary sits behind your parse, not in front of it — which after part 1 is exactly where your equatable model earns its keep. (If the parse itself is expensive enough to hurt, <code>SourceText.GetContentHash</code> gives you a cheap way to short-circuit manually, but for a config file that is rarely worth the ceremony.)</p>




<h2 id="collect-and-the-batching-trap"><a href="/posts/incremental-source-generators-pipeline-patterns/#collect-and-the-batching-trap" title="Collect and the Batching Trap">Collect and the Batching Trap</a></h2>
<p>Sometimes you need all models at once — to generate a single registration class, a module initializer, a combined lookup table. That is <code>.Collect()</code>, which turns an <code>IncrementalValuesProvider&lt;T&gt;</code> (many values) into an <code>IncrementalValueProvider&lt;ImmutableArray&lt;T&gt;&gt;</code> (one batched value).</p>
<p>And there it is again: <code>ImmutableArray&lt;T&gt;</code>. As covered at length in <a href="/posts/incremental-source-generators-done-right/">part 1</a>, <code>ImmutableArray&lt;T&gt;</code> has no value equality — two arrays with identical contents are not equal. So a <code>Collect</code> node produces a value that never compares equal to its previous version once any item changes, <em>and</em>, depending on what happens upstream, may hand you a fresh array instance even when no item changed. Every node downstream of a bare <code>Collect</code> is effectively uncached.</p>
<p>You have two idiomatic fixes. Either wrap the batch into the same <code>EquatableArray&lt;T&gt;</code> you already use inside your models:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">IncrementalValueProvider</span><span class="p">&lt;</span><span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">&gt;&gt;</span> <span class="n">allModels</span> <span class="p">=</span> <span class="n">models</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Collect</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="p">(</span><span class="n">array</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="k">new</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">&gt;(</span><span class="n">array</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">WithTrackingName</span><span class="p">(</span><span class="s">&#34;AllModels&#34;</span><span class="p">);</span>
</span></span></code></pre></div><p>Or keep the <code>ImmutableArray&lt;T&gt;</code> and attach an explicit comparer to the node with <code>WithComparer</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">IncrementalValueProvider</span><span class="p">&lt;</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">&gt;&gt;</span> <span class="n">allModels</span> <span class="p">=</span> <span class="n">models</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Collect</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">WithComparer</span><span class="p">(</span><span class="k">new</span> <span class="n">ImmutableArrayValueComparer</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">&gt;())</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">WithTrackingName</span><span class="p">(</span><span class="s">&#34;AllModels&#34;</span><span class="p">);</span>
</span></span></code></pre></div><p><code>ImmutableArrayValueComparer&lt;T&gt;</code> is not a Roslyn API — it is a ten-line helper you write yourself, an <code>IEqualityComparer&lt;ImmutableArray&lt;T&gt;&gt;</code> that walks both arrays and compares element-wise (the generators in CommunityToolkit.Mvvm ship exactly this shape):</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">class</span> <span class="nc">ImmutableArrayValueComparer</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="p">:</span> <span class="n">IEqualityComparer</span><span class="p">&lt;</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">bool</span> <span class="n">Equals</span><span class="p">(</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">x</span><span class="p">,</span> <span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">y</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="n">x</span><span class="p">.</span><span class="n">SequenceEqual</span><span class="p">(</span><span class="n">y</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">int</span> <span class="n">GetHashCode</span><span class="p">(</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">obj</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">hash</span> <span class="p">=</span> <span class="k">new</span> <span class="n">HashCode</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="k">foreach</span> <span class="p">(</span><span class="n">T</span> <span class="n">item</span> <span class="k">in</span> <span class="n">obj</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">hash</span><span class="p">.</span><span class="n">Add</span><span class="p">(</span><span class="n">item</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">hash</span><span class="p">.</span><span class="n">ToHashCode</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>I prefer the first: one equality mechanism everywhere, nothing to forget. You will also notice <code>WithTrackingName</code> sprinkled in. It does nothing at runtime — it names the pipeline node so that a test host running the <code>GeneratorDriver</code> with step tracking enabled can find the node in <code>GeneratorRunResult.TrackedSteps</code> (reached via <code>GeneratorDriverRunResult.Results</code>) and assert its <code>IncrementalStepRunReason</code> is <code>Cached</code> or <code>Unchanged</code>. That is the entire subject of <a href="/posts/incremental-source-generators-testing/">part 3</a>, so start naming your nodes now.</p>




<h2 id="a-complete-worked-generator"><a href="/posts/incremental-source-generators-pipeline-patterns/#a-complete-worked-generator" title="A Complete Worked Generator">A Complete Worked Generator</a></h2>
<p>Here is everything above assembled into one small but honest generator: <code>[AutoToString]</code> on a partial class generates a <code>ToString</code> override listing all public properties.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="na">[Generator]</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">class</span> <span class="nc">AutoToStringGenerator</span> <span class="p">:</span> <span class="n">IIncrementalGenerator</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="k">void</span> <span class="n">Initialize</span><span class="p">(</span><span class="n">IncrementalGeneratorInitializationContext</span> <span class="n">context</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">context</span><span class="p">.</span><span class="n">RegisterPostInitializationOutput</span><span class="p">(</span><span class="kd">static</span> <span class="n">ctx</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">            <span class="n">ctx</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="s">&#34;AutoToStringAttribute.g.cs&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">SourceText</span><span class="p">.</span><span class="n">From</span><span class="p">(</span><span class="n">AttributeSource</span><span class="p">,</span> <span class="n">Encoding</span><span class="p">.</span><span class="n">UTF8</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="n">IncrementalValuesProvider</span><span class="p">&lt;</span><span class="n">ClassModel</span><span class="p">&gt;</span> <span class="n">models</span> <span class="p">=</span> <span class="n">context</span><span class="p">.</span><span class="n">SyntaxProvider</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">ForAttributeWithMetadataName</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="s">&#34;AutoToString.AutoToStringAttribute&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">predicate</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">node</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">                    <span class="n">node</span> <span class="k">is</span> <span class="n">ClassDeclarationSyntax</span> <span class="n">cls</span>
</span></span><span class="line"><span class="cl">                    <span class="p">&amp;&amp;</span> <span class="n">cls</span><span class="p">.</span><span class="n">Modifiers</span><span class="p">.</span><span class="n">Any</span><span class="p">(</span><span class="n">SyntaxKind</span><span class="p">.</span><span class="n">PartialKeyword</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">                <span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">ct</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">                <span class="p">{</span>
</span></span><span class="line"><span class="cl">                    <span class="n">ct</span><span class="p">.</span><span class="n">ThrowIfCancellationRequested</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">                    <span class="k">if</span> <span class="p">(</span><span class="n">ctx</span><span class="p">.</span><span class="n">TargetSymbol</span> <span class="k">is</span> <span class="n">not</span> <span class="n">INamedTypeSymbol</span> <span class="n">symbol</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                    <span class="p">{</span>
</span></span><span class="line"><span class="cl">                        <span class="k">return</span> <span class="kc">null</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">                    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">                    <span class="k">return</span> <span class="k">new</span> <span class="n">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                        <span class="n">Namespace</span><span class="p">:</span> <span class="n">symbol</span><span class="p">.</span><span class="n">ContainingNamespace</span><span class="p">.</span><span class="n">IsGlobalNamespace</span>
</span></span><span class="line"><span class="cl">                            <span class="p">?</span> <span class="kc">null</span>
</span></span><span class="line"><span class="cl">                            <span class="p">:</span> <span class="n">symbol</span><span class="p">.</span><span class="n">ContainingNamespace</span><span class="p">.</span><span class="n">ToDisplayString</span><span class="p">(),</span>
</span></span><span class="line"><span class="cl">                        <span class="n">Name</span><span class="p">:</span> <span class="n">symbol</span><span class="p">.</span><span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                        <span class="n">Properties</span><span class="p">:</span> <span class="k">new</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;(</span>
</span></span><span class="line"><span class="cl">                            <span class="n">symbol</span><span class="p">.</span><span class="n">GetMembers</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">                                <span class="p">.</span><span class="n">OfType</span><span class="p">&lt;</span><span class="n">IPropertySymbol</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">                                <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="kd">static</span> <span class="n">p</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">                                    <span class="n">p</span><span class="p">.</span><span class="n">DeclaredAccessibility</span> <span class="p">==</span> <span class="n">Accessibility</span><span class="p">.</span><span class="n">Public</span>
</span></span><span class="line"><span class="cl">                                    <span class="p">&amp;&amp;</span> <span class="p">!</span><span class="n">p</span><span class="p">.</span><span class="n">IsStatic</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                                <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">Name</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">                                <span class="p">.</span><span class="n">ToImmutableArray</span><span class="p">()));</span>
</span></span><span class="line"><span class="cl">                <span class="p">})</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="kd">static</span> <span class="n">model</span> <span class="p">=&gt;</span> <span class="n">model</span> <span class="k">is</span> <span class="n">not</span> <span class="kc">null</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">WithTrackingName</span><span class="p">(</span><span class="s">&#34;Models&#34;</span><span class="p">)!;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="n">context</span><span class="p">.</span><span class="n">RegisterSourceOutput</span><span class="p">(</span><span class="n">models</span><span class="p">,</span> <span class="kd">static</span> <span class="p">(</span><span class="n">spc</span><span class="p">,</span> <span class="n">model</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="kt">var</span> <span class="n">builder</span> <span class="p">=</span> <span class="k">new</span> <span class="n">StringBuilder</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">&#34;// &lt;auto-generated/&gt;&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">&#34;#nullable enable&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(</span><span class="n">model</span><span class="p">.</span><span class="n">Namespace</span> <span class="k">is</span> <span class="n">not</span> <span class="kc">null</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">$&#34;namespace {model.Namespace};&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">                <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">            <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">$&#34;partial class {model.Name}&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">&#34;{&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">&#34;    public override string ToString() =&gt;&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">Append</span><span class="p">(</span><span class="s">$&#34;        $\&#34;</span><span class="p">{</span><span class="n">model</span><span class="p">.</span><span class="n">Name</span><span class="p">}</span> <span class="p">{{{{</span> <span class="s">&#34;);
</span></span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">Append</span><span class="p">(</span><span class="kt">string</span><span class="p">.</span><span class="n">Join</span><span class="p">(</span><span class="s">&#34;, &#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">model</span><span class="p">.</span><span class="n">Properties</span><span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="kd">static</span> <span class="n">p</span> <span class="p">=&gt;</span> <span class="s">$&#34;{p} = {{{p}}}&#34;</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">&#34; }}\&#34;;&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="n">builder</span><span class="p">.</span><span class="n">AppendLine</span><span class="p">(</span><span class="s">&#34;}&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="kt">string</span> <span class="n">hintName</span> <span class="p">=</span> <span class="n">model</span><span class="p">.</span><span class="n">Namespace</span> <span class="k">is</span> <span class="kc">null</span>
</span></span><span class="line"><span class="cl">                <span class="p">?</span> <span class="s">$&#34;{model.Name}.AutoToString.g.cs&#34;</span>
</span></span><span class="line"><span class="cl">                <span class="p">:</span> <span class="s">$&#34;{model.Namespace}.{model.Name}.AutoToString.g.cs&#34;</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="n">spc</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="n">hintName</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">SourceText</span><span class="p">.</span><span class="n">From</span><span class="p">(</span><span class="n">builder</span><span class="p">.</span><span class="n">ToString</span><span class="p">(),</span> <span class="n">Encoding</span><span class="p">.</span><span class="n">UTF8</span><span class="p">));</span>
</span></span><span class="line"><span class="cl">        <span class="p">});</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The supporting model, doing exactly what part 1 demands:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string?</span> <span class="n">Namespace</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span></code></pre></div><p>Trace the layers one more time: the attribute index pre-filters, the predicate confirms shape syntactically, the transform touches symbols once and emits a flat record, <code>Where</code> drops the failures, and the <code>StringBuilder</code> work lives exclusively in <code>RegisterSourceOutput</code> where it only runs when a <code>ClassModel</code> genuinely changed. The <code>hintName</code> includes the namespace so two classes with the same name in different namespaces do not collide — <code>AddSource</code> throws on duplicate hint names, and that failure mode always surfaces in someone else&rsquo;s codebase, never your test project.</p>




<h2 id="the-rules-in-one-place"><a href="/posts/incremental-source-generators-pipeline-patterns/#the-rules-in-one-place" title="The Rules, In One Place">The Rules, In One Place</a></h2>
<p>Everything in this post compresses to five lines:</p>
<ol>
<li>Enter through <code>ForAttributeWithMetadataName</code>, deliver the attribute via <code>RegisterPostInitializationOutput</code>.</li>
<li>Every pipeline lambda is <code>static</code>; the compiler is your capture police.</li>
<li>Predicate checks syntax, transform flattens to equatable records, <code>RegisterSourceOutput</code> builds strings.</li>
<li>Never <code>Combine</code> with <code>CompilationProvider</code> raw — <code>Select</code> the values you need out of it first.</li>
<li><code>Collect</code> produces <code>ImmutableArray&lt;T&gt;</code>; wrap it or attach a comparer before anything consumes it.</li>
</ol>
<p>Follow these and your generator will be incremental on the day you write it. Whether it <em>stays</em> incremental after six months of pull requests is a different question — one that only a test can answer.</p>
<p>Because none of this counts until a test proves the cache hits.</p>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/dotnet.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/dotnet.png" medium="image"/>
      <category>dotnet</category>
      <category>csharp</category>
      <category>roslyn</category>
      <category>sourcegenerators</category>
      <category>performance</category>
      <category>bestpractices</category>
    </item>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/incremental-source-generators-done-right/</guid>
      <link>https://daily-devops.net/posts/incremental-source-generators-done-right/</link>
      <title>Incremental Source Generators Done Right: The Equality Contract</title>
      <pubDate>Tue, 21 Jul 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>IIncrementalGenerator exists for one reason: to run only when its inputs actually change. Most generators fail at exactly that, because somewhere in the pipeline a value is compared by reference and the cache silently misses. This is part 1 of a 4-part series on getting incrementalism right, and it starts where every broken generator starts: the equality contract, the ImmutableArray trap, and the EquatableArray that fixes it.</description>
      <content:encoded><![CDATA[<p><a href="/posts/dotnet-source-generators-hidden-costs/">The previous post on source generators</a> was about the hidden costs — what generators take from you in build time, IDE responsiveness, and Hot Reload. The obvious follow-up question landed in my inbox more than once: <em>fine, can we at least do it right?</em></p>
<p>Yes. But &ldquo;right&rdquo; is more specific than the documentation lets on. <code>IIncrementalGenerator</code> exists for exactly one reason: to make sure your generator does real work only when its inputs change. Most generators in the wild do not achieve that. They implement the right interface, they compile cleanly, they pass their snapshot tests — and they still redo the full generation work on every edit, because somewhere in the pipeline a value is compared by reference and the cache silently misses.</p>
<p>This post opens a four-part series on getting incrementalism right, and all four parts share one running example: <code>[AutoToString]</code>, a small generator that gives a class a <code>ToString</code> override listing its public properties. This post uses its embryonic form to expose the equality contract; <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2</a> builds the generator properly.</p>




<h2 id="the-mental-model-a-pipeline-of-equality-checked-values"><a href="/posts/incremental-source-generators-done-right/#the-mental-model-a-pipeline-of-equality-checked-values" title="The Mental Model: A Pipeline of Equality-Checked Values">The Mental Model: A Pipeline of Equality-Checked Values</a></h2>
<p>Forget &ldquo;a generator is a method that emits code.&rdquo; An <code>IIncrementalGenerator</code> is a dataflow graph. Every node in the pipeline produces a value. The framework caches that value. On the next run, if the node&rsquo;s input is equal to the cached input — compared with <code>EqualityComparer&lt;T&gt;.Default</code> — the node does not execute and every node downstream of it stays cached too.</p>
<p>That is the entire architecture. There is no second mechanism. The whole promise of incrementalism rests on your values answering <code>Equals</code> correctly. If your equality is wrong, your incrementalism is wrong, and nothing in the compiler will tell you.</p>
<p>Here is the smallest honest skeleton — the embryonic <code>AutoToStringGenerator</code>, before it knows anything about properties:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="na">[Generator]</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">class</span> <span class="nc">AutoToStringGenerator</span> <span class="p">:</span> <span class="n">IIncrementalGenerator</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="k">void</span> <span class="n">Initialize</span><span class="p">(</span><span class="n">IncrementalGeneratorInitializationContext</span> <span class="n">context</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="c1">// Node 1: one string per attributed class. Cached per value.</span>
</span></span><span class="line"><span class="cl">        <span class="n">IncrementalValuesProvider</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">classNames</span> <span class="p">=</span> <span class="n">context</span><span class="p">.</span><span class="n">SyntaxProvider</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">ForAttributeWithMetadataName</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="s">&#34;AutoToString.AutoToStringAttribute&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">predicate</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">node</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">node</span> <span class="k">is</span> <span class="n">ClassDeclarationSyntax</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">transform</span><span class="p">:</span> <span class="kd">static</span> <span class="p">(</span><span class="n">ctx</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">ctx</span><span class="p">.</span><span class="n">TargetSymbol</span><span class="p">.</span><span class="n">Name</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Node 2: only re-runs for a class whose name actually changed.</span>
</span></span><span class="line"><span class="cl">        <span class="n">context</span><span class="p">.</span><span class="n">RegisterSourceOutput</span><span class="p">(</span><span class="n">classNames</span><span class="p">,</span> <span class="kd">static</span> <span class="p">(</span><span class="n">spc</span><span class="p">,</span> <span class="n">className</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">            <span class="n">spc</span><span class="p">.</span><span class="n">AddSource</span><span class="p">(</span><span class="s">$&#34;{className}.AutoToString.g.cs&#34;</span><span class="p">,</span> <span class="err">$</span><span class="s">$&#34;&#34;&#34;
</span></span></span><span class="line"><span class="cl"><span class="s">                partial class {{className}}
</span></span></span><span class="line"><span class="cl"><span class="s">                {
</span></span></span><span class="line"><span class="cl"><span class="s">                    public override string ToString() =&gt; &#34;</span><span class="p">{{</span><span class="n">className</span><span class="p">}}</span> <span class="p">{</span> <span class="p">}</span><span class="s">&#34;;
</span></span></span><span class="line"><span class="cl">                <span class="p">}</span>
</span></span><span class="line"><span class="cl">                <span class="s">&#34;&#34;&#34;));
</span></span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>This one is bulletproof — as a <em>caching</em> skeleton, and for a boring reason: the only thing flowing between the nodes is a <code>string</code>. Strings have value equality. Type a character in an unrelated file, the transform produces the same string, <code>EqualityComparer&lt;string&gt;.Default</code> says equal, <code>RegisterSourceOutput</code> never runs. (As an <code>AutoToString</code> implementation it is deliberately embryonic: it knows only the class name, so the <code>ToString</code> it emits lists no properties, and because it ignores the containing namespace, the emitted partial only merges with classes in the global namespace. The marker attribute has to come from somewhere too — <a href="/posts/incremental-source-generators-pipeline-patterns/">part 2</a> delivers it via <code>RegisterPostInitializationOutput</code> and fixes the rest.)</p>
<p>The finished generator does not get away with a single string: a <code>ToString</code> override worth generating needs the property names too. The moment you carry more than one piece of data per target, you reach for a <code>record</code> with a collection property — and that is where the contract quietly breaks.</p>




<h2 id="the-default-you-will-get-wrong-first"><a href="/posts/incremental-source-generators-done-right/#the-default-you-will-get-wrong-first" title="The Default You Will Get Wrong First">The Default You Will Get Wrong First</a></h2>
<p>The obvious next step for <code>AutoToString</code> is a record holding the class name and its public property names — the very model the finished generator caches. This first draft looks correct. It compiles. It even feels idiomatic:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// This looks fine. It is not.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">ClassModel</span><span class="p">(</span><span class="kt">string</span> <span class="n">Name</span><span class="p">,</span> <span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span></code></pre></div><p>A <code>record</code> gives you value equality on its members, so <code>Name</code> is fine. But <code>ImmutableArray&lt;T&gt;</code> does <strong>not</strong> implement value equality. It is a struct wrapping a reference to a <code>T[]</code>, and its <code>Equals</code> compares that inner array <em>reference</em> — not the contents. Two arrays holding identical strings are not equal:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">a</span> <span class="p">=</span> <span class="n">ImmutableArray</span><span class="p">.</span><span class="n">Create</span><span class="p">(</span><span class="s">&#34;Id&#34;</span><span class="p">,</span> <span class="s">&#34;Name&#34;</span><span class="p">,</span> <span class="s">&#34;CreatedAt&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">b</span> <span class="p">=</span> <span class="n">ImmutableArray</span><span class="p">.</span><span class="n">Create</span><span class="p">(</span><span class="s">&#34;Id&#34;</span><span class="p">,</span> <span class="s">&#34;Name&#34;</span><span class="p">,</span> <span class="s">&#34;CreatedAt&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">Console</span><span class="p">.</span><span class="n">WriteLine</span><span class="p">(</span><span class="n">a</span><span class="p">.</span><span class="n">SequenceEqual</span><span class="p">(</span><span class="n">b</span><span class="p">));</span> <span class="c1">// True  - same contents</span>
</span></span><span class="line"><span class="cl"><span class="n">Console</span><span class="p">.</span><span class="n">WriteLine</span><span class="p">(</span><span class="n">a</span><span class="p">.</span><span class="n">Equals</span><span class="p">(</span><span class="n">b</span><span class="p">));</span>        <span class="c1">// False - different underlying arrays</span>
</span></span><span class="line"><span class="cl"><span class="n">Console</span><span class="p">.</span><span class="n">WriteLine</span><span class="p">(</span><span class="n">a</span> <span class="p">==</span> <span class="n">b</span><span class="p">);</span>             <span class="c1">// False - same reference comparison</span>
</span></span></code></pre></div><p>Now follow the consequence through the pipeline. Your transform runs on every edit (that part is by design — Roslyn has to re-check what changed). Each run builds a <em>fresh</em> <code>ImmutableArray&lt;string&gt;</code> from the symbol&rsquo;s members. Fresh array, fresh reference, <code>Equals</code> returns <code>false</code>, the record&rsquo;s synthesized equality returns <code>false</code>, the cache misses, and everything downstream — including your string-building, formatting, <code>AddSource</code> — re-executes. Every single time. In every project that references your generator.</p>
<p>The generator is still &ldquo;incremental&rdquo; in the sense that it implements the interface. It is non-incremental in every sense that matters. And you will not notice until someone opens a binlog, <a href="/posts/dotnet-source-generators-hidden-costs/">like I did in the predecessor post</a>, and finds your generator at the top of the <code>RunGenerators</code> list.</p>




<h2 id="the-fix-equatablearrayt"><a href="/posts/incremental-source-generators-done-right/#the-fix-equatablearrayt" title="The Fix: EquatableArray&lt;T&gt;">The Fix: EquatableArray&lt;T&gt;</a></h2>
<p>The fix is a small wrapper that gives an array the equality semantics the pipeline needs. This is the centerpiece of part 1 — copy it into your generator project as-is:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">System</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">System.Collections</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">System.Collections.Generic</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">System.Collections.Immutable</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">System.Runtime.CompilerServices</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="cs">/// &lt;summary&gt;</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// An immutable array with value-based equality, safe to use</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// in incremental generator pipeline models.</span>
</span></span><span class="line"><span class="cl"><span class="cs">/// &lt;/summary&gt;</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">readonly</span> <span class="k">struct</span> <span class="nc">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="p">:</span> <span class="n">IEquatable</span><span class="p">&lt;</span><span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;&gt;,</span> <span class="n">IEnumerable</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="k">where</span> <span class="n">T</span> <span class="p">:</span> <span class="n">IEquatable</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="k">readonly</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">Empty</span> <span class="p">=</span> <span class="k">new</span><span class="p">(</span><span class="n">Array</span><span class="p">.</span><span class="n">Empty</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;());</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="k">readonly</span> <span class="n">T</span><span class="p">[]?</span> <span class="n">_array</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">EquatableArray</span><span class="p">(</span><span class="n">T</span><span class="p">[]</span> <span class="n">array</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="n">_array</span> <span class="p">=</span> <span class="n">array</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">EquatableArray</span><span class="p">(</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">array</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">:</span> <span class="k">this</span><span class="p">(</span><span class="n">Unsafe</span><span class="p">.</span><span class="n">As</span><span class="p">&lt;</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;,</span> <span class="n">T</span><span class="p">[]?&gt;(</span><span class="k">ref</span> <span class="n">array</span><span class="p">)</span> <span class="p">??</span> <span class="n">Array</span><span class="p">.</span><span class="n">Empty</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;())</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">int</span> <span class="n">Count</span> <span class="p">=&gt;</span> <span class="n">_array</span><span class="p">?.</span><span class="n">Length</span> <span class="p">??</span> <span class="m">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">ReadOnlySpan</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">AsSpan</span><span class="p">()</span> <span class="p">=&gt;</span> <span class="n">_array</span><span class="p">.</span><span class="n">AsSpan</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">bool</span> <span class="n">Equals</span><span class="p">(</span><span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">other</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="n">AsSpan</span><span class="p">().</span><span class="n">SequenceEqual</span><span class="p">(</span><span class="n">other</span><span class="p">.</span><span class="n">AsSpan</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">override</span> <span class="kt">bool</span> <span class="n">Equals</span><span class="p">(</span><span class="kt">object?</span> <span class="n">obj</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="n">obj</span> <span class="k">is</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">other</span> <span class="p">&amp;&amp;</span> <span class="n">Equals</span><span class="p">(</span><span class="n">other</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">override</span> <span class="kt">int</span> <span class="n">GetHashCode</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="c1">// A default instance (null _array) is Equals-equal to Empty,</span>
</span></span><span class="line"><span class="cl">        <span class="c1">// so it must hash like an empty array too.</span>
</span></span><span class="line"><span class="cl">        <span class="k">if</span> <span class="p">(</span><span class="n">_array</span> <span class="k">is</span> <span class="kc">null</span> <span class="p">||</span> <span class="n">_array</span><span class="p">.</span><span class="n">Length</span> <span class="p">==</span> <span class="m">0</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="m">17</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">unchecked</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="kt">var</span> <span class="n">hash</span> <span class="p">=</span> <span class="m">17</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">            <span class="k">foreach</span> <span class="p">(</span><span class="n">T</span> <span class="n">item</span> <span class="k">in</span> <span class="n">_array</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">            <span class="p">{</span>
</span></span><span class="line"><span class="cl">                <span class="n">hash</span> <span class="p">=</span> <span class="p">(</span><span class="n">hash</span> <span class="p">*</span> <span class="m">31</span><span class="p">)</span> <span class="p">+</span> <span class="p">(</span><span class="n">item</span><span class="p">?.</span><span class="n">GetHashCode</span><span class="p">()</span> <span class="p">??</span> <span class="m">0</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">            <span class="k">return</span> <span class="n">hash</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">IEnumerator</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">GetEnumerator</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="p">((</span><span class="n">IEnumerable</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;)(</span><span class="n">_array</span> <span class="p">??</span> <span class="n">Array</span><span class="p">.</span><span class="n">Empty</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;())).</span><span class="n">GetEnumerator</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">IEnumerator</span> <span class="n">IEnumerable</span><span class="p">.</span><span class="n">GetEnumerator</span><span class="p">()</span> <span class="p">=&gt;</span> <span class="n">GetEnumerator</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">bool</span> <span class="kd">operator</span> <span class="p">==(</span><span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">left</span><span class="p">,</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">right</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="n">left</span><span class="p">.</span><span class="n">Equals</span><span class="p">(</span><span class="n">right</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">bool</span> <span class="kd">operator</span> <span class="p">!=(</span><span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">left</span><span class="p">,</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">right</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">=&gt;</span> <span class="p">!</span><span class="n">left</span><span class="p">.</span><span class="n">Equals</span><span class="p">(</span><span class="n">right</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">implicit</span> <span class="kd">operator</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;(</span><span class="n">T</span><span class="p">[]</span> <span class="n">array</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="k">new</span><span class="p">(</span><span class="n">array</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">static</span> <span class="kd">implicit</span> <span class="kd">operator</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;(</span><span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;</span> <span class="n">array</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="k">new</span><span class="p">(</span><span class="n">array</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Three details worth knowing before you paste:</p>
<ul>
<li><code>Equals</code> uses <code>SequenceEqual</code> over spans — element-by-element, no allocation. <code>GetHashCode</code> combines all elements, because a hash based on the array reference would be just as broken as the equality we started with.</li>
<li>The <code>ImmutableArray&lt;T&gt;</code> constructor uses <code>Unsafe.As</code> to reuse the inner array without copying. Generators target <code>netstandard2.0</code>, so you reference the <a href="https://www.nuget.org/packages/System.Memory" target="_blank" rel="noopener external noreferrer">System.Memory</a> package — it provides the <code>ReadOnlySpan&lt;T&gt;</code> and <code>AsSpan</code> APIs the struct leans on, and it transitively brings in <a href="https://www.nuget.org/packages/System.Runtime.CompilerServices.Unsafe" target="_blank" rel="noopener external noreferrer">System.Runtime.CompilerServices.Unsafe</a> for the cast. If the trick makes you nervous, <code>array.ToArray()</code> works too — you pay one copy per transform run, which is still infinitely cheaper than a permanent cache miss.</li>
<li>This is not an exotic pattern I invented. It is what Microsoft ships inside its own generators — the <code>System.Text.Json</code> source generator in <a href="https://github.com/dotnet/runtime/blob/main/src/libraries/Common/src/SourceGenerators/ImmutableEquatableArray.cs" target="_blank" rel="noopener external noreferrer">dotnet/runtime</a> carries an <code>ImmutableEquatableArray&lt;T&gt;</code> for precisely this reason, and the <a href="https://github.com/CommunityToolkit/dotnet" target="_blank" rel="noopener external noreferrer">CommunityToolkit</a> generators carry their own <code>EquatableArray&lt;T&gt;</code>. When the people who built the pipeline all wrap their arrays, take the hint.</li>
</ul>
<p>The fixed model is a one-word change:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Now the record&#39;s synthesized equality is correct all the way down.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">ClassModel</span><span class="p">(</span><span class="kt">string</span> <span class="n">Name</span><span class="p">,</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span></code></pre></div>



<h2 id="strip-to-the-smallest-equatable-shape-early"><a href="/posts/incremental-source-generators-done-right/#strip-to-the-smallest-equatable-shape-early" title="Strip to the Smallest Equatable Shape Early">Strip to the Smallest Equatable Shape Early</a></h2>
<p>The array trap is the most common break, but it is a special case of a general rule: everything that flows through the pipeline must be value-equatable, and it must be <em>small</em>. My rules for a pipeline model:</p>
<ul>
<li>It is a <code>record</code>, so equality is synthesized member-wise.</li>
<li>Every member is a primitive, a <code>string</code>, an <code>enum</code>, another equatable record, or an <code>EquatableArray&lt;T&gt;</code> of those.</li>
<li>It contains <strong>no</strong> <code>INamedTypeSymbol</code>, no <code>Compilation</code>, no <code>SyntaxNode</code>, no <code>SemanticModel</code>. Ever.</li>
</ul>
<p>Side by side, the broken shape and the one that survives caching:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Broken: symbols are reference-equal across compilations.</span>
</span></span><span class="line"><span class="cl"><span class="c1">// This cache can never hit after an edit.</span>
</span></span><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">BrokenTarget</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">INamedTypeSymbol</span> <span class="n">Type</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="n">IPropertySymbol</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Fixed: nothing but value-equatable members.</span>
</span></span><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">Namespace</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="kt">string</span> <span class="n">AccessModifier</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">Properties</span><span class="p">);</span>
</span></span></code></pre></div><p>Compared with the two-member draft above, this shape has grown a namespace — emission needs it to write the <code>namespace</code> declaration — and an access modifier, which is here to make the general point: carry whatever the emitter needs, flattened to strings. Part 2&rsquo;s final version slims the record back down to namespace, name, and properties, because that is all the <code>ToString</code> emitter actually consumes. The contract does not care how many members the record has, only that every one of them compares by value.</p>
<p>The conversion from symbol world to value world happens exactly once, inside the <code>transform</code> of <code>ForAttributeWithMetadataName</code>. That is the last point in the pipeline where a symbol is allowed to exist:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">private</span> <span class="kd">static</span> <span class="n">ClassModel</span> <span class="n">Create</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">    <span class="n">GeneratorAttributeSyntaxContext</span> <span class="n">context</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">CancellationToken</span> <span class="n">cancellationToken</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="n">symbol</span> <span class="p">=</span> <span class="p">(</span><span class="n">INamedTypeSymbol</span><span class="p">)</span><span class="n">context</span><span class="p">.</span><span class="n">TargetSymbol</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">ImmutableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;</span> <span class="n">propertyNames</span> <span class="p">=</span> <span class="n">symbol</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">GetMembers</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">OfType</span><span class="p">&lt;</span><span class="n">IPropertySymbol</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">DeclaredAccessibility</span> <span class="p">==</span> <span class="n">Accessibility</span><span class="p">.</span><span class="n">Public</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">Select</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">Name</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">ToImmutableArray</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// Flatten ONCE. Nothing past this line ever sees a symbol again.</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="k">new</span> <span class="n">ClassModel</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">symbol</span><span class="p">.</span><span class="n">ContainingNamespace</span><span class="p">.</span><span class="n">ToDisplayString</span><span class="p">(),</span>
</span></span><span class="line"><span class="cl">        <span class="n">symbol</span><span class="p">.</span><span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">SyntaxFacts</span><span class="p">.</span><span class="n">GetText</span><span class="p">(</span><span class="n">symbol</span><span class="p">.</span><span class="n">DeclaredAccessibility</span><span class="p">),</span>
</span></span><span class="line"><span class="cl">        <span class="k">new</span> <span class="n">EquatableArray</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;(</span><span class="n">propertyNames</span><span class="p">));</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Yes, this transform re-runs whenever the attributed class is edited. That is fine — it is cheap, and it is <em>supposed</em> to run then. What matters is what happens on every other edit: the transform produces a <code>ClassModel</code> that is value-equal to the cached one, and the expensive downstream work — code building, formatting, emitting — never executes.</p>




<h2 id="why-symbols-poison-the-cache"><a href="/posts/incremental-source-generators-done-right/#why-symbols-poison-the-cache" title="Why Symbols Poison the Cache">Why Symbols Poison the Cache</a></h2>
<p>It is worth understanding <em>why</em> the symbol rule exists, because &ldquo;just don&rsquo;t&rdquo; ages badly and the broken version looks so harmless:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Looks like a convenient cache key. It is a slow memory leak</span>
</span></span><span class="line"><span class="cl"><span class="c1">// wearing a cache miss as a disguise.</span>
</span></span><span class="line"><span class="cl"><span class="kd">internal</span> <span class="kd">sealed</span> <span class="k">record</span> <span class="nc">CachedModel</span><span class="p">(</span><span class="n">INamedTypeSymbol</span> <span class="n">Type</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="c1">// roots: the symbol</span>
</span></span><span class="line"><span class="cl"><span class="c1">//   -&gt; its ContainingModule and ContainingAssembly</span>
</span></span><span class="line"><span class="cl"><span class="c1">//   -&gt; the Compilation that produced it</span>
</span></span><span class="line"><span class="cl"><span class="c1">//   -&gt; every syntax tree, metadata reference, and semantic</span>
</span></span><span class="line"><span class="cl"><span class="c1">//      model bound to that compilation snapshot</span>
</span></span></code></pre></div><p>Two independent problems, either of which is disqualifying.</p>
<p><strong>First, correctness.</strong> <code>ISymbol</code> instances are reference-equal per compilation. Every keystroke produces a new <code>Compilation</code>, and the new compilation produces <em>new</em> symbol instances for the same declarations. Your <code>CachedModel</code> from the previous run holds the old symbol; the transform produces a new one; <code>EqualityComparer&lt;CachedModel&gt;.Default</code> compares them, the record delegates to the symbol&rsquo;s reference equality, and the answer is <code>false</code>. Permanent cache miss, by construction. (Roslyn does have <code>SymbolEqualityComparer.Default</code> for comparing symbols deliberately — but the pipeline does not use it, and it would not fix the second problem anyway.)</p>
<p><strong>Second, memory.</strong> The pipeline cache holds your model objects across runs — that is its job. If the model holds a symbol, the cache is now rooting the entire compilation snapshot the symbol came from, as the comment trail above spells out. Syntax trees, metadata references, semantic models: none of it can be collected while your &ldquo;small cached record&rdquo; is alive. Do this in a generator that ships in a popular package and you have built a memory leak into every Visual Studio and Rider instance that opens a consuming solution. Roslyn&rsquo;s own <a href="https://github.com/dotnet/roslyn/blob/main/docs/features/incremental-generators.cookbook.md" target="_blank" rel="noopener external noreferrer">incrementality guidance</a> calls this out for the same reason: symbols and compilations must never be captured in pipeline state.</p>
<p>Strings are boring. Boring is the point. A <code>ClassModel</code> made of strings costs a few hundred bytes, compares in nanoseconds, and roots nothing.</p>




<h2 id="the-contract"><a href="/posts/incremental-source-generators-done-right/#the-contract" title="The Contract">The Contract</a></h2>
<p>Here it is in one paragraph. Every value flowing through an <code>IIncrementalGenerator</code> pipeline is cached and compared with <code>EqualityComparer&lt;T&gt;.Default</code>; therefore every value must be a small, immutable, value-equatable shape — records of primitives and <code>EquatableArray&lt;T&gt;</code>, flattened from symbols exactly once inside the transform, with no <code>ISymbol</code>, <code>Compilation</code>, or <code>SyntaxNode</code> surviving past that point. Honor that and the framework does the caching for you. Break it anywhere, even in one property of one record, and your generator runs on every keystroke while looking perfectly incremental in the source.</p>
<p>That is the foundation. Everything that follows in this series — the shape of the pipeline, the test that proves the cache actually hits, the package that survives contact with consuming projects — builds on this one contract. Get it wrong here and no operator, test, or packaging trick downstream will save you.</p>
<blockquote>
<p>Equality first. <strong>Everything else is downstream.</strong></p>
</blockquote>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/dotnet.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/dotnet.png" medium="image"/>
      <category>dotnet</category>
      <category>csharp</category>
      <category>roslyn</category>
      <category>sourcegenerators</category>
      <category>performance</category>
      <category>bestpractices</category>
    </item>
  </channel>
</rss>
