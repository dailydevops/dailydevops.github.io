<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:media="http://search.yahoo.com/mrss/"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>C# 15 Language Features in Practice on Daily DevOps &amp; .NET</title>
    <link>https://daily-devops.net/tags/csharp15/</link>
    <description>Recent content in C# 15 Language Features in Practice on Daily DevOps &amp; .NET</description>
    <language>en</language>
    <copyright>© 2026 Daily DevOps &amp; .NET</copyright>
    <lastBuildDate>Thu, 20 Aug 2026 01:44:44 +0000</lastBuildDate>
    <image>
      <url>https://daily-devops.net/images/logo_hu_295faa9e530b1c14.png</url>
      <title>C# 15 Language Features in Practice on Daily DevOps &amp; .NET</title>
      <link>https://daily-devops.net/tags/csharp15/</link>
      <width>144</width>
      <height>144</height>
    </image>
    <atom:link type="application/rss+xml" href="https://daily-devops.net/tags/csharp15/feed.rss" hreflang="en" rel="self"/>
    <atom:link type="text/html" href="https://daily-devops.net/tags/csharp15/" hreflang="en" rel="alternate"/>
    <atom:link type="application/atom+xml" href="https://daily-devops.net/tags/csharp15/feed.atom" hreflang="en" rel="alternate"/>
    <atom:link type="application/feed+json" href="https://daily-devops.net/tags/csharp15/feed.json" hreflang="en" rel="alternate"/>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/csharp-15-union-types-expectations/</guid>
      <link>https://daily-devops.net/posts/csharp-15-union-types-expectations/</link>
      <title>C# 15 Union Types and Closed Hierarchies in Preview</title>
      <pubDate>Wed, 19 Aug 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>C# 15 preview ships union types with real compiler-checked exhaustiveness, no more hand-rolled sealed hierarchies for a Result type. The catch: it is a union of pre-existing types, not F#-style discriminated cases, and value-type cases box unless you write non-boxing boilerplate yourself. The quieter sibling feature, closed hierarchies, may be the safer bet for enterprise teams right now.</description>
      <content:encoded><![CDATA[<p>If you have ever hand-rolled a <code>Result</code> type as a sealed abstract class with two private-constructor subclasses, or reached for the <a href="https://github.com/mcintyre321/OneOf" target="_blank" rel="noopener external noreferrer">OneOf</a> NuGet package because the language gave you nothing better, you already know the problem C# 15&rsquo;s union types are aimed at. That gap has existed since a 2017 csharplang proposal first floated the idea, and it is finally something you can install and try.</p>
<p>This is the first of a short series looking at C# 15 candidate and preview language features: what they are, where the design actually stands, and whether they earn a spot in a production codebase or just a spot in a conference talk.</p>




<h2 id="what-shipped-and-where"><a href="/posts/csharp-15-union-types-expectations/#what-shipped-and-where" title="What Shipped, and Where">What Shipped, and Where</a></h2>
<p>Two related but separately shippable features landed together in .NET 11 preview SDKs, with official documentation on Microsoft Learn covering <a href="https://learn.microsoft.com/dotnet/csharp/language-reference/builtin-types/union" target="_blank" rel="noopener external noreferrer">union types</a> and the <a href="https://learn.microsoft.com/dotnet/csharp/language-reference/proposals/csharp-15.0/unions" target="_blank" rel="noopener external noreferrer">unions proposal</a>: nominal <code>union</code> type declarations, and a <code>closed</code> modifier for exhaustive class hierarchies. The runtime plumbing for unions (an <code>IUnion</code> interface and supporting attributes) arrived in .NET 11 Preview 5, and you need <code>LangVersion=preview</code> to compile any of it. Design work continues on the working group&rsquo;s <a href="https://github.com/dotnet/csharplang/issues/8928" target="_blank" rel="noopener external noreferrer">champion issue #8928</a>, driven by a dedicated working group since roughly 2022, with Fred Silberberg as the named champion alongside Mads Torgersen and Matt Warren.</p>
<p>Here is the syntax as documented today:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="n">union</span> <span class="n">Pet</span><span class="p">(</span><span class="n">Cat</span><span class="p">,</span> <span class="n">Dog</span><span class="p">,</span> <span class="n">Bird</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">Cat</span><span class="p">(</span><span class="kt">string</span> <span class="n">Name</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">Dog</span><span class="p">(</span><span class="kt">string</span> <span class="n">Name</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">Bird</span><span class="p">(</span><span class="kt">string</span> <span class="n">Name</span><span class="p">);</span>
</span></span></code></pre></div><p>That single line generates a struct implementing <code>IUnion</code> with an implicit constructor for each case type. Pattern matching over it looks exactly like a normal <code>switch</code> expression, except the compiler now understands the closed set of possibilities:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">name</span> <span class="p">=</span> <span class="n">pet</span> <span class="k">switch</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">Dog</span> <span class="n">d</span> <span class="p">=&gt;</span> <span class="n">d</span><span class="p">.</span><span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">Cat</span> <span class="n">c</span> <span class="p">=&gt;</span> <span class="n">c</span><span class="p">.</span><span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">    <span class="n">Bird</span> <span class="n">b</span> <span class="p">=&gt;</span> <span class="n">b</span><span class="p">.</span><span class="n">Name</span><span class="p">,</span>
</span></span><span class="line"><span class="cl"><span class="p">};</span>
</span></span></code></pre></div><p>No <code>default</code> arm required. Add a fourth case type to the union declaration later, forget to update this switch, and the compiler warns you. That single behavior, compiler-verified exhaustiveness, is the entire pitch, and it&rsquo;s a legitimate one. Nothing before this gave you that for free.</p>
<p>Case types are not limited to concrete classes. Generic type parameters work too, which is where the error-handling use case becomes obvious:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="n">union</span> <span class="n">Result</span><span class="p">&lt;</span><span class="n">TSuccess</span><span class="p">,</span> <span class="n">TError</span><span class="p">&gt;(</span><span class="n">TSuccess</span><span class="p">,</span> <span class="n">TError</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">None</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">Some</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;(</span><span class="n">T</span> <span class="n">Value</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="n">union</span> <span class="n">Option</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;(</span><span class="n">None</span><span class="p">,</span> <span class="n">Some</span><span class="p">&lt;</span><span class="n">T</span><span class="p">&gt;);</span>
</span></span></code></pre></div><p>A <code>Result&lt;TSuccess, TError&gt;</code> return type forces every caller to handle both the success and error case in a switch, with the compiler flagging anything unhandled. That is the exact shape you were building by hand with a sealed abstract class and two private-constructor subclasses, or with OneOf.</p>




<h2 id="the-catch-unions-of-types-not-of-cases"><a href="/posts/csharp-15-union-types-expectations/#the-catch-unions-of-types-not-of-cases" title="The Catch: Unions of Types, Not of Cases">The Catch: Unions of Types, Not of Cases</a></h2>
<p>Here is where I want to slow down, because the enthusiasm around this feature, mine very much included, runs ahead of what it actually is. The <a href="https://learn.microsoft.com/dotnet/csharp/language-reference/builtin-types/union" target="_blank" rel="noopener external noreferrer">official Microsoft Learn documentation</a> says it plainly: unions in C# are unions <strong>of types</strong>, not discriminated or tagged unions in the F# sense.</p>
<p>That distinction matters more than it sounds. In F#, a discriminated union declares its cases and their fields inline as part of the type itself:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-fsharp" data-lang="fsharp"><span class="line"><span class="cl"><span class="k">type</span> <span class="nc">Shape</span> <span class="o">=</span>
</span></span><span class="line"><span class="cl">    <span class="o">|</span> <span class="n">Circle</span> <span class="k">of</span> <span class="kt">float</span>
</span></span><span class="line"><span class="cl">    <span class="o">|</span> <span class="n">Rectangle</span> <span class="k">of</span> <span class="kt">float</span> <span class="o">*</span> <span class="kt">float</span>
</span></span></code></pre></div><p>There is no separate <code>Circle</code> type floating around your codebase; the case and its data are defined together, once, as part of the union. C#&rsquo;s <code>union Pet(Cat, Dog, Bird)</code> is a union of three pre-existing, independently usable types. That is a real feature, and a useful one, but it is not &ldquo;F# discriminated unions with C# syntax,&rdquo; which is roughly the pitch I would have made myself before reading past the headline. You still design and maintain <code>Cat</code>, <code>Dog</code>, and <code>Bird</code> as ordinary types; the union just wraps them.</p>
<p>The second catch is more concrete and more likely to bite you in production: boxing. The compiler-generated struct behind the one-line <code>union</code> declaration always stores its contents as <code>object?</code>, so any value-type case gets boxed on every access:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Every read of the int or double case boxes it into object?.</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="n">union</span> <span class="n">Measurement</span><span class="p">(</span><span class="kt">int</span><span class="p">,</span> <span class="kt">double</span><span class="p">);</span>
</span></span></code></pre></div><p>To avoid that, you do not get a flag or an attribute to opt out. You either abandon the one-line declaration and hand-write the union struct yourself, or keep the <code>union</code> keyword and delegate to a nested <code>IUnionMembers</code> interface, writing only the <code>TryGetValue</code> overloads. Either way, implementing <code>IUnion</code> (directly or through the provider interface) plus a <code>HasValue</code> property and a <code>TryGetValue(out T)</code> overload per case is the non-boxing access pattern the official docs describe:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="na">[System.Runtime.CompilerServices.Union]</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">struct</span> <span class="nc">Measurement</span> <span class="p">:</span> <span class="n">System</span><span class="p">.</span><span class="n">Runtime</span><span class="p">.</span><span class="n">CompilerServices</span><span class="p">.</span><span class="n">IUnion</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="k">readonly</span> <span class="kt">int</span> <span class="n">_meters</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="k">readonly</span> <span class="kt">double</span> <span class="n">_kilometers</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="k">readonly</span> <span class="kt">byte</span> <span class="n">_tag</span><span class="p">;</span> <span class="c1">// 0 = none, 1 = meters, 2 = kilometers</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">Measurement</span><span class="p">(</span><span class="kt">int</span> <span class="k">value</span><span class="p">)</span> <span class="p">{</span> <span class="n">_meters</span> <span class="p">=</span> <span class="k">value</span><span class="p">;</span> <span class="n">_tag</span> <span class="p">=</span> <span class="m">1</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">Measurement</span><span class="p">(</span><span class="kt">double</span> <span class="k">value</span><span class="p">)</span> <span class="p">{</span> <span class="n">_kilometers</span> <span class="p">=</span> <span class="k">value</span><span class="p">;</span> <span class="n">_tag</span> <span class="p">=</span> <span class="m">2</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">object?</span> <span class="n">Value</span> <span class="p">=&gt;</span> <span class="n">_tag</span> <span class="k">switch</span> <span class="p">{</span> <span class="m">1</span> <span class="p">=&gt;</span> <span class="n">_meters</span><span class="p">,</span> <span class="m">2</span> <span class="p">=&gt;</span> <span class="n">_kilometers</span><span class="p">,</span> <span class="n">_</span> <span class="p">=&gt;</span> <span class="kc">null</span> <span class="p">};</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">bool</span> <span class="n">HasValue</span> <span class="p">=&gt;</span> <span class="n">_tag</span> <span class="p">!=</span> <span class="m">0</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">bool</span> <span class="n">TryGetValue</span><span class="p">(</span><span class="k">out</span> <span class="kt">int</span> <span class="k">value</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">value</span> <span class="p">=</span> <span class="n">_meters</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">_tag</span> <span class="p">==</span> <span class="m">1</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kt">bool</span> <span class="n">TryGetValue</span><span class="p">(</span><span class="k">out</span> <span class="kt">double</span> <span class="k">value</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">value</span> <span class="p">=</span> <span class="n">_kilometers</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="n">_tag</span> <span class="p">==</span> <span class="m">2</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Sit with that for a second. The entire point of a union type, in most enterprise pitches I have heard, is &ldquo;less boilerplate than OneOf or a hand-rolled Result type.&rdquo; For struct case types where allocation matters, you write almost the same boilerplate back in: it just moves from a third-party package to your own codebase. That is not nothing (a compiler-recognized shape for it is worth something), but it is not the free lunch I was hoping for.</p>
<p>There is also a genuine null footgun for struct unions: if the union&rsquo;s value could be <code>null</code>, your switch needs an explicit <code>null =&gt;</code> arm, or the compiler will complain. We just spent a decade teaching developers that &ldquo;the compiler will tell you if something can be null&rdquo; is progress. Reintroducing a null-shaped hole in a brand-new type category, even with a diagnostic backstop, is the kind of thing I would happily skim past in a proposal doc and then rediscover six months later in a Slack thread titled &ldquo;why is this switch not exhaustive.&rdquo;</p>




<h2 id="the-underrated-half-closed-hierarchies"><a href="/posts/csharp-15-union-types-expectations/#the-underrated-half-closed-hierarchies" title="The Underrated Half: closed Hierarchies">The Underrated Half: <code>closed</code> Hierarchies</a></h2>
<p>If I had to bet enterprise budget on one of these two features today, it would not be <code>union</code>. It would be <code>closed</code>.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="n">closed</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">PaymentMethod</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">Cash</span> <span class="p">:</span> <span class="n">PaymentMethod</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">CreditCard</span><span class="p">(</span><span class="kt">string</span> <span class="n">Last4</span><span class="p">)</span> <span class="p">:</span> <span class="n">PaymentMethod</span><span class="p">;</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">record</span> <span class="nc">class</span> <span class="n">BankTransfer</span><span class="p">(</span><span class="kt">string</span> <span class="n">Iban</span><span class="p">)</span> <span class="p">:</span> <span class="n">PaymentMethod</span><span class="p">;</span>
</span></span></code></pre></div><p>This is not a new runtime concept. It is plain classes and records, marked with a modifier that tells the compiler &ldquo;I am declaring the complete set of subtypes, right here, and I want you to enforce it.&rdquo; Switch expressions over a <code>closed</code> hierarchy get the same exhaustiveness diagnostics as <code>union</code> (<a href="https://learn.microsoft.com/dotnet/csharp/language-reference/proposals/csharp-15.0/closed-hierarchies" target="_blank" rel="noopener external noreferrer">the proposal</a> cites <code>CS8509</code> and <code>CS8510</code> for missing arms), but without a new runtime type, without an <code>IUnion</code> interface, without boxing questions, and without a null-arm surprise. It is closer to what a lot of teams are already doing with sealed abstract classes and private constructors, just with the compiler finally checking the thing that developer discipline was checking manually.</p>
<p>If your domain models a fixed set of <em>behaviorally distinct types with their own methods and inheritance</em>, reach for <code>closed</code>. If it models a fixed set of <em>interchangeable data shapes you switch over</em>, <code>union</code> is the better fit, boxing caveats included. Expect both idioms to coexist in the same codebase for a while, and expect a debate about which one a given <code>Result&lt;T, TError&gt;</code>-shaped return type should use. I do not think that debate resolves cleanly before GA.</p>




<h2 id="my-expectation-and-the-risk"><a href="/posts/csharp-15-union-types-expectations/#my-expectation-and-the-risk" title="My Expectation, and the Risk">My Expectation, and the Risk</a></h2>
<p><strong>What I expect to be genuinely useful:</strong> the <code>closed</code> modifier, on day one, for teams already modeling state machines and result types with sealed hierarchies. It formalizes a pattern most senior C# developers already reach for manually and gives it real diagnostics. Low migration cost, low conceptual overhead, no new runtime surface.</p>
<p><strong>What I expect to be overhyped:</strong> <code>union</code> understood as &ldquo;F# for C# developers.&rdquo; It is not case-level pattern matching with inline fields; it is a compiler-recognized wrapper around existing types, with real allocation trade-offs for value-type cases that require you to hand-write the same non-boxing boilerplate you were trying to escape.</p>
<p><strong>What could bite enterprise teams specifically:</strong></p>
<ul>
<li><strong>Timing.</strong> This missed C# 14 / .NET 10 entirely. It is targeted at C# 15 / .NET 11, with a November 2026 GA date reported by community sources (notably <a href="https://blog.maartenballiauw.be/posts/2026-06-16-discriminated-unions-in-csharp-for-real-this-time/" target="_blank" rel="noopener external noreferrer">Maarten Balliauw&rsquo;s June 2026 write-up</a>), but that date is not confirmed on an official Microsoft release-schedule page as of this writing. Plan roadmaps around what has actually shipped, not around a date that has not yet appeared on an official release schedule.</li>
<li><strong>Design churn.</strong> As of this writing, only 4 of the 8 linked sub-proposals on the working group&rsquo;s <a href="https://github.com/dotnet/csharplang/issues/8928" target="_blank" rel="noopener external noreferrer">champion issue</a> are closed (Custom Unions, Nominal Type Unions, the Non-Boxing Access Pattern, and Union Interfaces). Case Declarations, Closed Enums, Closed Hierarchies, and Standard Unions are still open. Core switch and pattern-matching semantics look stable across preview builds, but reflection support and serialization behavior remain unresolved open questions, and an ad hoc/anonymous union syntax (<code>A or B or C</code>) is under discussion and not confirmed for the C# 15 GA scope at all. Preview means preview: do not put this in a library&rsquo;s public API surface yet.</li>
<li><strong>Idiom fragmentation.</strong> OneOf, hand-rolled <code>Result&lt;T&gt;</code> types, sealed hierarchies, and now two new first-class options. Pick one direction per codebase and document why, or your code review comments will spend the next two years litigating which pattern a new PR should use.</li>
</ul>




<h2 id="practical-takeaway"><a href="/posts/csharp-15-union-types-expectations/#practical-takeaway" title="Practical Takeaway">Practical Takeaway</a></h2>
<p>Do not migrate anything to <code>union</code> today; it is preview software with a fifth of its design questions still open, and the boxing story alone deserves a team discussion before adoption, not after. Do watch <code>closed</code> hierarchies closely: they are the lower-risk piece, they map onto patterns your team likely already uses, and they cost almost nothing to adopt once .NET 11 is generally available. When it does ship, budget time for a &ldquo;which idiom for which domain shape&rdquo; decision inside your team&rsquo;s coding guidelines, not for a wholesale OneOf-to-union migration on day one. The compiler-checked exhaustiveness is real progress. The &ldquo;just like F#&rdquo; framing is not, and that distinction is worth carrying into the next architecture discussion.</p>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/csharp.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/csharp.png" medium="image"/>
      <category>dotnet</category>
      <category>csharp</category>
      <category>csharp15</category>
      <category>softwareengineering</category>
    </item>
  </channel>
</rss>
