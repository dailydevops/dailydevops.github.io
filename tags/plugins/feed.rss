<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0"
     xmlns:atom="http://www.w3.org/2005/Atom"
     xmlns:media="http://search.yahoo.com/mrss/"
     xmlns:content="http://purl.org/rss/1.0/modules/content/"
     xmlns:dc="http://purl.org/dc/elements/1.1/">
  <channel>
    <title>Plugin and Extensibility Architecture on Daily DevOps &amp; .NET</title>
    <link>https://daily-devops.net/tags/plugins/</link>
    <description>Recent content in Plugin and Extensibility Architecture on Daily DevOps &amp; .NET</description>
    <language>en</language>
    <copyright>© 2026 Daily DevOps &amp; .NET</copyright>
    <lastBuildDate>Mon, 13 Jul 2026 08:39:47 +0000</lastBuildDate>
    <image>
      <url>https://daily-devops.net/images/logo_hu_295faa9e530b1c14.png</url>
      <title>Plugin and Extensibility Architecture on Daily DevOps &amp; .NET</title>
      <link>https://daily-devops.net/tags/plugins/</link>
      <width>144</width>
      <height>144</height>
    </image>
    <atom:link type="application/rss+xml" href="https://daily-devops.net/tags/plugins/feed.rss" hreflang="en" rel="self"/>
    <atom:link type="text/html" href="https://daily-devops.net/tags/plugins/" hreflang="en" rel="alternate"/>
    <atom:link type="application/atom+xml" href="https://daily-devops.net/tags/plugins/feed.atom" hreflang="en" rel="alternate"/>
    <atom:link type="application/feed+json" href="https://daily-devops.net/tags/plugins/feed.json" hreflang="en" rel="alternate"/>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/efcore-excludefrommigrations-2-the-solution/</guid>
      <link>https://daily-devops.net/posts/efcore-excludefrommigrations-2-the-solution/</link>
      <title>EF Core Plugins: Owning Migrations with ExcludeFromMigrations</title>
      <pubDate>Thu, 09 Jul 2026 17:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>ExcludeFromMigrations() keeps plugin entities fully queryable in the host DbContext while stripping migration ownership of their tables. Combined with per-context migration history tables, IDesignTimeDbContextFactory, and an explicit application-layer data access pattern, it enables truly independent plugin schema evolution.</description>
      <content:encoded><![CDATA[<p>In <a href="/posts/efcore-excludefrommigrations-1-the-problem/">Part 1</a>, I showed how EF Core&rsquo;s entity discovery pulls plugin tables into the host migration. The fix is <code>ExcludeFromMigrations()</code>, introduced in EF Core 5.0 and stable through EF Core 10. But the method name is slightly misleading: it does not remove the entity from the model. It removes migration ownership of the table. EF Core still knows the entity exists, still lets you query it, and still tracks changes. It just stops generating <code>CREATE</code>, <code>ALTER</code>, and <code>DROP</code> statements for that table in the current context&rsquo;s migrations.</p>
<p>That distinction matters a lot. You keep the full EF Core query surface while ceding schema control back to whoever actually owns the table.</p>




<h2 id="what-excludefrommigrations-does-and-does-not-do"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#what-excludefrommigrations-does-and-does-not-do" title="What ExcludeFromMigrations Does and Does Not Do">What ExcludeFromMigrations Does and Does Not Do</a></h2>
<p>The API lives on <code>TableBuilder&lt;TEntity&gt;</code>:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">modelBuilder</span><span class="p">.</span><span class="n">Entity</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">ToTable</span><span class="p">(</span><span class="s">&#34;Posts&#34;</span><span class="p">,</span> <span class="n">schema</span><span class="p">:</span> <span class="s">&#34;blog&#34;</span><span class="p">,</span> <span class="n">t</span> <span class="p">=&gt;</span> <span class="n">t</span><span class="p">.</span><span class="n">ExcludeFromMigrations</span><span class="p">());</span>
</span></span></code></pre></div><p>After this call:</p>
<ul>
<li>Migrations generated from this <code>DbContext</code> will not include any DDL (data definition language) for <code>blog.Posts</code></li>
<li><code>dbContext.Set&lt;Post&gt;()</code> still works; queries translate to SQL normally</li>
<li>Change tracking, <code>SaveChanges</code>, raw SQL: all intact</li>
<li>EF Core&rsquo;s model snapshot records the entity, so subsequent migrations do not flip-flop about whether it exists</li>
</ul>
<p>Two version notes worth keeping in mind. In EF Core 7, passing <code>null</code> as the table name throws <code>ArgumentNullException</code>: the name is mandatory. In EF Core 8, a TPC (table-per-concrete-type) hierarchy bug was fixed: previously, excluding one table in a TPC inheritance hierarchy accidentally excluded all sibling tables. Since 8.0, only the specific table you target is excluded.</p>




<h2 id="the-fk-navigation-gotcha"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#the-fk-navigation-gotcha" title="The FK Navigation Gotcha">The FK Navigation Gotcha</a></h2>
<p>Here is where developers hit the wall. EF Core GitHub issue <a href="https://github.com/dotnet/efcore/issues/23639" target="_blank" rel="noopener external noreferrer">#23639</a> describes the behavior: when the host <code>DbContext</code> has a <strong>configured navigation property</strong> pointing at an excluded entity, EF Core regenerates <code>DROP FOREIGN KEY</code> and <code>ADD FOREIGN KEY</code> operations on every subsequent migration, even when nothing has changed. The migration diff incorrectly thinks the FK (foreign key) needs recreation.</p>
<p>The fix is unambiguous: <strong>do not configure navigation properties across context boundaries</strong>. The host context should reference plugin entities by foreign key column value only. The entity type stays in the model for queries, but no relationship is configured.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Bad: navigation property across context boundary</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">Page</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">Guid</span> <span class="n">Id</span> <span class="p">{</span> <span class="k">get</span><span class="p">;</span> <span class="k">set</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">ICollection</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;</span> <span class="n">RelatedPosts</span> <span class="p">{</span> <span class="k">get</span><span class="p">;</span> <span class="k">set</span><span class="p">;</span> <span class="p">}</span> <span class="p">=</span> <span class="p">[];</span>  <span class="c1">// do not do this</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Correct: FK column value only</span>
</span></span><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">Page</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">Guid</span> <span class="n">Id</span> <span class="p">{</span> <span class="k">get</span><span class="p">;</span> <span class="k">set</span><span class="p">;</span> <span class="p">}</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// No navigation. Reference Post.PageId from the application layer.</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>If you need to load related data, do it in the application layer as a separate query. More on that shortly.</p>




<h2 id="layer-1-host-context-excludes-plugin-tables"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#layer-1-host-context-excludes-plugin-tables" title="Layer 1: Host Context Excludes Plugin Tables">Layer 1: Host Context Excludes Plugin Tables</a></h2>
<p>The host <code>DbContext</code> can still know about plugin entities for raw queries, reporting joins, and dashboard aggregates. It just does not own the tables:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">CmsDbContext</span><span class="p">(</span><span class="n">DbContextOptions</span><span class="p">&lt;</span><span class="n">CmsDbContext</span><span class="p">&gt;</span> <span class="n">options</span><span class="p">)</span> <span class="p">:</span> <span class="n">DbContext</span><span class="p">(</span><span class="n">options</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">DbSet</span><span class="p">&lt;</span><span class="n">Page</span><span class="p">&gt;</span> <span class="n">Pages</span> <span class="p">=&gt;</span> <span class="n">Set</span><span class="p">&lt;</span><span class="n">Page</span><span class="p">&gt;();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">protected</span> <span class="kd">override</span> <span class="k">void</span> <span class="n">OnModelCreating</span><span class="p">(</span><span class="n">ModelBuilder</span> <span class="n">modelBuilder</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">modelBuilder</span><span class="p">.</span><span class="n">HasDefaultSchema</span><span class="p">(</span><span class="s">&#34;cms&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Plugin entity: queryable, not migration-managed, no navigation configured</span>
</span></span><span class="line"><span class="cl">        <span class="n">modelBuilder</span><span class="p">.</span><span class="n">Entity</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">ToTable</span><span class="p">(</span><span class="s">&#34;Posts&#34;</span><span class="p">,</span> <span class="n">schema</span><span class="p">:</span> <span class="s">&#34;blog&#34;</span><span class="p">,</span> <span class="n">t</span> <span class="p">=&gt;</span> <span class="n">t</span><span class="p">.</span><span class="n">ExcludeFromMigrations</span><span class="p">());</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Now <code>dotnet ef migrations add</code> on the host project generates DDL only for <code>cms.*</code> tables. The <code>blog.Posts</code> table appears in the EF model snapshot but produces no migration output.</p>




<h2 id="layer-2-plugin-context-owns-its-tables"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#layer-2-plugin-context-owns-its-tables" title="Layer 2: Plugin Context Owns Its Tables">Layer 2: Plugin Context Owns Its Tables</a></h2>
<p>The plugin ships its own <code>DbContext</code> with full ownership of its schema. No <code>ExcludeFromMigrations</code> needed here. This context is the authoritative owner:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">BlogPluginDbContext</span><span class="p">(</span><span class="n">DbContextOptions</span><span class="p">&lt;</span><span class="n">BlogPluginDbContext</span><span class="p">&gt;</span> <span class="n">options</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">:</span> <span class="n">DbContext</span><span class="p">(</span><span class="n">options</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">DbSet</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;</span> <span class="n">Posts</span> <span class="p">=&gt;</span> <span class="n">Set</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;();</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">DbSet</span><span class="p">&lt;</span><span class="n">Tag</span><span class="p">&gt;</span> <span class="n">Tags</span> <span class="p">=&gt;</span> <span class="n">Set</span><span class="p">&lt;</span><span class="n">Tag</span><span class="p">&gt;();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">protected</span> <span class="kd">override</span> <span class="k">void</span> <span class="n">OnModelCreating</span><span class="p">(</span><span class="n">ModelBuilder</span> <span class="n">modelBuilder</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">modelBuilder</span><span class="p">.</span><span class="n">HasDefaultSchema</span><span class="p">(</span><span class="s">&#34;blog&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="n">modelBuilder</span><span class="p">.</span><span class="n">Entity</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;(</span><span class="n">e</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="n">e</span><span class="p">.</span><span class="n">HasKey</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">Id</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">            <span class="c1">// FK by value only; no navigation to CmsUser from the host context</span>
</span></span><span class="line"><span class="cl">            <span class="n">e</span><span class="p">.</span><span class="n">Property</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">AuthorId</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="p">});</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The <code>HasDefaultSchema(&quot;blog&quot;)</code> call is critical. Without it, table names collide with the host schema when both use SQL Server&rsquo;s <code>dbo</code> default. Each context needs its own schema to prevent ambiguity in both naming and migration isolation.</p>




<h3 id="migration-isolation-via-idesigntimedbcontextfactory"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#migration-isolation-via-idesigntimedbcontextfactory" title="Migration Isolation via IDesignTimeDbContextFactory">Migration Isolation via IDesignTimeDbContextFactory</a></h3>
<p>For <code>dotnet ef migrations add</code> to work from the plugin project at design time, the plugin needs an <code>IDesignTimeDbContextFactory</code>. This tells the EF tooling how to instantiate the context independently of the host DI container:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">BlogPluginDbContextFactory</span> <span class="p">:</span> <span class="n">IDesignTimeDbContextFactory</span><span class="p">&lt;</span><span class="n">BlogPluginDbContext</span><span class="p">&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">BlogPluginDbContext</span> <span class="n">CreateDbContext</span><span class="p">(</span><span class="kt">string</span><span class="p">[]</span> <span class="n">args</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">options</span> <span class="p">=</span> <span class="k">new</span> <span class="n">DbContextOptionsBuilder</span><span class="p">&lt;</span><span class="n">BlogPluginDbContext</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">UseSqlServer</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                <span class="s">&#34;Server=.;Database=CmsDb;Trusted_Connection=True;&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">                <span class="n">x</span> <span class="p">=&gt;</span> <span class="n">x</span><span class="p">.</span><span class="n">MigrationsAssembly</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">                           <span class="k">typeof</span><span class="p">(</span><span class="n">BlogPluginDbContextFactory</span><span class="p">).</span><span class="n">Assembly</span><span class="p">.</span><span class="n">GetName</span><span class="p">().</span><span class="n">Name</span><span class="p">!)</span>
</span></span><span class="line"><span class="cl">                       <span class="p">.</span><span class="n">MigrationsHistoryTable</span><span class="p">(</span><span class="s">&#34;__BlogMigrations&#34;</span><span class="p">,</span> <span class="s">&#34;blog&#34;</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">            <span class="p">.</span><span class="n">Options</span><span class="p">;</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="k">return</span> <span class="k">new</span> <span class="n">BlogPluginDbContext</span><span class="p">(</span><span class="n">options</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The <code>MigrationsHistoryTable</code> call is not optional. By default, every EF context writes applied migrations to <code>__EFMigrationsHistory</code> in the default schema. If both the host and the plugin share a database, they collide on that table: the host sees plugin migration entries and vice versa, producing false &ldquo;already applied&rdquo; errors on deployment. Isolating the history table per context keeps each migration ledger clean and independent.</p>
<p>Plugin migrations run from the plugin project:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="line"><span class="cl"><span class="n">dotnet</span> <span class="n">ef</span> <span class="n">migrations</span> <span class="n">add</span> <span class="n">InitialBlogSchema</span> <span class="p">-</span><span class="n">-context</span> <span class="n">BlogPluginDbContext</span> <span class="p">`</span>
</span></span><span class="line"><span class="cl">  <span class="p">-</span><span class="n">-project</span> <span class="n">BlogPlugin</span><span class="p">.</span><span class="py">Migrations</span>
</span></span></code></pre></div><p>Host migrations run from the host project:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-powershell" data-lang="powershell"><span class="line"><span class="cl"><span class="n">dotnet</span> <span class="n">ef</span> <span class="n">migrations</span> <span class="n">add</span> <span class="n">InitialCms</span> <span class="p">-</span><span class="n">-context</span> <span class="n">CmsDbContext</span> <span class="p">`</span>
</span></span><span class="line"><span class="cl">  <span class="p">-</span><span class="n">-project</span> <span class="n">CmsHost</span>
</span></span></code></pre></div><p>Independent timelines, independent tooling, independent history tables.</p>




<h3 id="host-registration"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#host-registration" title="Host Registration">Host Registration</a></h3>
<p>When registering both contexts at startup, the host needs the plugin&rsquo;s migration assembly and history table:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">builder</span><span class="p">.</span><span class="n">Services</span><span class="p">.</span><span class="n">AddDbContext</span><span class="p">&lt;</span><span class="n">CmsDbContext</span><span class="p">&gt;(</span><span class="n">options</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="n">options</span><span class="p">.</span><span class="n">UseSqlServer</span><span class="p">(</span><span class="n">connectionString</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">x</span> <span class="p">=&gt;</span> <span class="n">x</span><span class="p">.</span><span class="n">MigrationsHistoryTable</span><span class="p">(</span><span class="s">&#34;__CmsMigrations&#34;</span><span class="p">,</span> <span class="s">&#34;cms&#34;</span><span class="p">)));</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="n">builder</span><span class="p">.</span><span class="n">Services</span><span class="p">.</span><span class="n">AddDbContext</span><span class="p">&lt;</span><span class="n">BlogPluginDbContext</span><span class="p">&gt;(</span><span class="n">options</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="n">options</span><span class="p">.</span><span class="n">UseSqlServer</span><span class="p">(</span><span class="n">connectionString</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">x</span> <span class="p">=&gt;</span> <span class="n">x</span><span class="p">.</span><span class="n">MigrationsAssembly</span><span class="p">(</span><span class="s">&#34;BlogPlugin.Migrations&#34;</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">               <span class="p">.</span><span class="n">MigrationsHistoryTable</span><span class="p">(</span><span class="s">&#34;__BlogMigrations&#34;</span><span class="p">,</span> <span class="s">&#34;blog&#34;</span><span class="p">)));</span>
</span></span></code></pre></div>



<h2 id="layer-3-cross-context-data-in-the-application-layer"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#layer-3-cross-context-data-in-the-application-layer" title="Layer 3: Cross-Context Data in the Application Layer">Layer 3: Cross-Context Data in the Application Layer</a></h2>
<p>With navigation properties removed across context boundaries, loading related data requires two queries. This is an explicit data access pattern that makes the context boundary visible in code, which is actually a feature rather than a limitation:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="kd">async</span> <span class="n">Task</span><span class="p">&lt;</span><span class="n">PostWithAuthorDto</span><span class="p">&gt;</span> <span class="n">GetPostWithAuthorAsync</span><span class="p">(</span><span class="n">Guid</span> <span class="n">postId</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="n">post</span> <span class="p">=</span> <span class="k">await</span> <span class="n">_blogDb</span><span class="p">.</span><span class="n">Posts</span><span class="p">.</span><span class="n">FindAsync</span><span class="p">(</span><span class="n">postId</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="kt">var</span> <span class="n">author</span> <span class="p">=</span> <span class="k">await</span> <span class="n">_cmsDb</span><span class="p">.</span><span class="n">Users</span><span class="p">.</span><span class="n">FindAsync</span><span class="p">(</span><span class="n">post</span><span class="p">!.</span><span class="n">AuthorId</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="k">return</span> <span class="k">new</span> <span class="n">PostWithAuthorDto</span><span class="p">(</span><span class="n">post</span><span class="p">,</span> <span class="n">author</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>For reporting scenarios where you need JOIN-like semantics, use the host context&rsquo;s excluded entity reference directly in LINQ. EF Core generates the SQL JOIN even without a navigation property, as long as both tables are accessible on the same connection:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kt">var</span> <span class="n">results</span> <span class="p">=</span> <span class="k">await</span> <span class="n">_cmsDb</span><span class="p">.</span><span class="n">Set</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Where</span><span class="p">(</span><span class="n">p</span> <span class="p">=&gt;</span> <span class="n">p</span><span class="p">.</span><span class="n">PublishedAt</span> <span class="p">&gt;</span> <span class="n">DateTime</span><span class="p">.</span><span class="n">UtcNow</span><span class="p">.</span><span class="n">AddDays</span><span class="p">(-</span><span class="m">30</span><span class="p">))</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Join</span><span class="p">(</span><span class="n">_cmsDb</span><span class="p">.</span><span class="n">Pages</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">          <span class="n">post</span> <span class="p">=&gt;</span> <span class="n">post</span><span class="p">.</span><span class="n">PageId</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">          <span class="n">page</span> <span class="p">=&gt;</span> <span class="n">page</span><span class="p">.</span><span class="n">Id</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">          <span class="p">(</span><span class="n">post</span><span class="p">,</span> <span class="n">page</span><span class="p">)</span> <span class="p">=&gt;</span> <span class="k">new</span> <span class="p">{</span> <span class="n">post</span><span class="p">.</span><span class="n">Title</span><span class="p">,</span> <span class="n">page</span><span class="p">.</span><span class="n">Slug</span> <span class="p">})</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">ToListAsync</span><span class="p">();</span>
</span></span></code></pre></div><p>This works because <code>Post</code> is still in the <code>CmsDbContext</code> model via <code>ExcludeFromMigrations</code>. EF Core can generate SQL for it; it just will not generate DDL for it.</p>




<h2 id="startup-migration-ordering"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#startup-migration-ordering" title="Startup Migration Ordering">Startup Migration Ordering</a></h2>
<p>When applying migrations at startup, order matters. Host tables often serve as the parent side of foreign keys that plugins reference. Apply the host first:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="k">using</span> <span class="nn">var</span> <span class="n">scope</span> <span class="p">=</span> <span class="n">app</span><span class="p">.</span><span class="n">Services</span><span class="p">.</span><span class="n">CreateScope</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Host first: cms.Users, cms.Pages, etc. must exist before plugin FKs resolve</span>
</span></span><span class="line"><span class="cl"><span class="k">await</span> <span class="n">scope</span><span class="p">.</span><span class="n">ServiceProvider</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">GetRequiredService</span><span class="p">&lt;</span><span class="n">CmsDbContext</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Database</span><span class="p">.</span><span class="n">MigrateAsync</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl"><span class="c1">// Plugin second: blog.Posts may FK into cms.Users</span>
</span></span><span class="line"><span class="cl"><span class="k">await</span> <span class="n">scope</span><span class="p">.</span><span class="n">ServiceProvider</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">GetRequiredService</span><span class="p">&lt;</span><span class="n">BlogPluginDbContext</span><span class="p">&gt;()</span>
</span></span><span class="line"><span class="cl">    <span class="p">.</span><span class="n">Database</span><span class="p">.</span><span class="n">MigrateAsync</span><span class="p">();</span>
</span></span></code></pre></div><p>For plugin seed data, prefer EF Core 9&rsquo;s <code>UseSeeding</code> over <code>HasData</code>. <code>HasData</code> embeds seed records in migrations, which creates timeline ordering conflicts between contexts when they share a database. <code>UseSeeding</code> runs after <code>MigrateAsync</code> and is decoupled from migration history entirely:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="n">services</span><span class="p">.</span><span class="n">AddDbContext</span><span class="p">&lt;</span><span class="n">BlogPluginDbContext</span><span class="p">&gt;(</span><span class="n">options</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">    <span class="n">options</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">UseSqlServer</span><span class="p">(</span><span class="n">connectionString</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">        <span class="p">.</span><span class="n">UseSeeding</span><span class="p">((</span><span class="n">context</span><span class="p">,</span> <span class="n">_</span><span class="p">)</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl">        <span class="p">{</span>
</span></span><span class="line"><span class="cl">            <span class="k">if</span> <span class="p">(!</span><span class="n">context</span><span class="p">.</span><span class="n">Set</span><span class="p">&lt;</span><span class="n">BlogCategory</span><span class="p">&gt;().</span><span class="n">Any</span><span class="p">())</span>
</span></span><span class="line"><span class="cl">                <span class="n">context</span><span class="p">.</span><span class="n">Set</span><span class="p">&lt;</span><span class="n">BlogCategory</span><span class="p">&gt;().</span><span class="n">Add</span><span class="p">(</span><span class="k">new</span> <span class="n">BlogCategory</span> <span class="p">{</span> <span class="n">Name</span> <span class="p">=</span> <span class="s">&#34;General&#34;</span> <span class="p">});</span>
</span></span><span class="line"><span class="cl">            <span class="n">context</span><span class="p">.</span><span class="n">SaveChanges</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="p">}));</span>
</span></span></code></pre></div>



<h2 id="integration-testing"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#integration-testing" title="Integration Testing">Integration Testing</a></h2>
<p>With each context owning its own migration history, testing follows the same separation. The host context and plugin context apply their migrations independently in test setup:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">BlogPluginTests</span> <span class="p">:</span> <span class="n">IAsyncLifetime</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">private</span> <span class="k">readonly</span> <span class="n">MsSqlContainer</span> <span class="n">_db</span> <span class="p">=</span> <span class="k">new</span> <span class="n">MsSqlBuilder</span><span class="p">().</span><span class="n">Build</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="kd">async</span> <span class="n">Task</span> <span class="n">InitializeAsync</span><span class="p">()</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="k">await</span> <span class="n">_db</span><span class="p">.</span><span class="n">StartAsync</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">        <span class="kt">var</span> <span class="n">connStr</span> <span class="p">=</span> <span class="n">_db</span><span class="p">.</span><span class="n">GetConnectionString</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Host tables must exist first (plugin may FK into them)</span>
</span></span><span class="line"><span class="cl">        <span class="k">await</span> <span class="k">using</span> <span class="nn">var</span> <span class="n">cmsCtx</span> <span class="p">=</span> <span class="n">CreateCmsContext</span><span class="p">(</span><span class="n">connStr</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">await</span> <span class="n">cmsCtx</span><span class="p">.</span><span class="n">Database</span><span class="p">.</span><span class="n">MigrateAsync</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">        <span class="c1">// Plugin applies its own migrations independently</span>
</span></span><span class="line"><span class="cl">        <span class="k">await</span> <span class="k">using</span> <span class="nn">var</span> <span class="n">blogCtx</span> <span class="p">=</span> <span class="n">CreateBlogContext</span><span class="p">(</span><span class="n">connStr</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">        <span class="k">await</span> <span class="n">blogCtx</span><span class="p">.</span><span class="n">Database</span><span class="p">.</span><span class="n">MigrateAsync</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">Task</span> <span class="n">DisposeAsync</span><span class="p">()</span> <span class="p">=&gt;</span> <span class="n">_db</span><span class="p">.</span><span class="n">DisposeAsync</span><span class="p">().</span><span class="n">AsTask</span><span class="p">();</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>For plugin-only tests where the host context is not relevant, you can skip <code>CmsDbContext.MigrateAsync()</code> entirely, but you will need to manually create any tables the plugin&rsquo;s <code>ExcludeFromMigrations</code> entities reference, since those tables will not exist. A minimal <code>DbContext</code> that only maps what the test needs is the cleanest approach for fully isolated plugin testing.</p>




<h2 id="when-to-use-this-pattern-vs-a-unified-context"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#when-to-use-this-pattern-vs-a-unified-context" title="When to Use This Pattern vs. a Unified Context">When to Use This Pattern vs. a Unified Context</a></h2>
<p>The alternative is a unified context: one <code>DbContext</code> that calls each module&rsquo;s <code>OnModelCreating</code> registration method, merging all modules into one migration timeline. One <code>dotnet ef migrations add</code> covers everything, and startup ordering concerns disappear.</p>
<p>The cost is coupling. A broken model configuration in one module blocks migrations for every other module. Teams cannot evolve schemas independently. Removing a module requires surgery on the shared migration history. You cannot just uninstall a plugin and move on.</p>
<p>The <code>ExcludeFromMigrations</code> pattern prioritizes <strong>plugin autonomy</strong> over <strong>operational simplicity</strong>. Choose it when:</p>
<ul>
<li>Plugins need to evolve schemas on their own timeline</li>
<li>Plugin installation and removal must be self-contained operations</li>
<li>Plugin authors should not need to coordinate schema changes with the host team</li>
<li>Third-party or community plugins are part of the design</li>
</ul>
<p>Choose a unified context when:</p>
<ul>
<li>You control all modules and cross-team coordination is not a constraint</li>
<li>Deployment simplicity outweighs module independence</li>
<li>The module set is stable and schema changes are infrequent</li>
</ul>
<p>The decision maps to organizational structure and deployment philosophy more than technical correctness.</p>




<h2 id="the-complete-picture"><a href="/posts/efcore-excludefrommigrations-2-the-solution/#the-complete-picture" title="The Complete Picture">The Complete Picture</a></h2>
<p><code>ExcludeFromMigrations()</code> is precise. It removes DDL ownership without touching query capability. The full pattern is:</p>
<ul>
<li><strong>Host context</strong>: <code>ExcludeFromMigrations()</code> for each plugin entity, no navigation properties across context boundaries</li>
<li><strong>Plugin context</strong>: owns its schema with <code>HasDefaultSchema</code>, separate migrations assembly, isolated <code>MigrationsHistoryTable</code></li>
<li><strong>Design-time tooling</strong>: <code>IDesignTimeDbContextFactory</code> per plugin context for independent <code>dotnet ef</code> commands</li>
<li><strong>Application layer</strong>: explicit multi-context queries instead of cross-context navigations</li>
<li><strong>Startup</strong>: host migrations before plugin migrations</li>
</ul>
<p>That is a foundation for plugin-based systems that can evolve their schemas independently, install and uninstall cleanly, and stay decoupled from the host&rsquo;s migration timeline.</p>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/extensions.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/extensions.png" medium="image"/>
      <category>efcore</category>
      <category>dotnet</category>
      <category>csharp</category>
      <category>architecture</category>
      <category>plugins</category>
    </item>
    <item>
      <guid isPermaLink="true">https://daily-devops.net/posts/efcore-excludefrommigrations-1-the-problem/</guid>
      <link>https://daily-devops.net/posts/efcore-excludefrommigrations-1-the-problem/</link>
      <title>EF Core Plugins: When Migrations Go Wrong</title>
      <pubDate>Tue, 07 Jul 2026 23:00:00 +0200</pubDate>
      <dc:creator>Martin Stühmer</dc:creator>
      <description>EF Core’s entity discovery is thorough by design. In a plugin architecture, that thoroughness becomes a liability: navigation properties pull plugin entities into the host migration, creating shared schema ownership that breaks plugin isolation, causes schema drift, and makes clean plugin uninstalls impossible.</description>
      <content:encoded><![CDATA[<p>Picture this: your ASP.NET Core application is well-structured, extensible by design. A blog plugin ships its own entities, its own business logic. The host application references the plugin assembly for navigation purposes and everything compiles cleanly. Then you run <code>dotnet ef migrations add InitialMigration</code> from the host project, and the generated migration cheerfully creates <code>blog.Posts</code>, <code>blog.Tags</code>, and every other table the plugin owns. The host just annexed your plugin&rsquo;s schema.</p>
<p>This is not a contrived edge case. It happens in modular monoliths, SaaS platforms with optional feature modules, content management systems, and any extensible host that shares a database with its plugins. The root cause is mundane: EF Core is doing exactly what it was designed to do. The problem is that nobody told it where the host&rsquo;s responsibility ends.</p>




<h2 id="how-ef-core-discovers-entities"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#how-ef-core-discovers-entities" title="How EF Core Discovers Entities">How EF Core Discovers Entities</a></h2>
<p>When <code>OnModelCreating</code> runs, EF Core does not stop at the types you explicitly registered via <code>DbSet&lt;T&gt;</code> properties. It traverses navigation properties. If your host <code>DbContext</code> has a <code>DbSet&lt;Page&gt;</code>, and <code>Page</code> has a navigation property to a <code>Post</code> (which belongs to the blog plugin), EF Core follows that relationship and includes <code>Post</code> in the model. From EF Core&rsquo;s perspective, it found an entity type, and it adds it to the migration.</p>
<p>This behavior is intentional and generally useful. In a straightforward application with a single context, you want EF Core to discover related entities automatically. But in a plugin architecture, this auto-discovery crosses a boundary it should not cross.</p>
<p>The same thing happens when a host <code>DbContext</code> has an explicit <code>modelBuilder.Entity&lt;Post&gt;()</code> call (perhaps to configure a query filter or join behavior) without any instruction about who owns the table. EF Core registers it, includes it in migrations, and now the host migration file contains <code>CREATE TABLE [blog].[Posts]</code>.</p>
<p>This rarely shows up on day one. The trigger is typically a later refactor: someone adds a <code>Post? LatestPost</code> navigation property to <code>Page</code> so the dashboard can show a preview. Nobody touches <code>OnModelCreating</code>, and yet the next <code>dotnet ef migrations add</code> silently pulls in the entire <code>Post</code> graph, which is why it survives code review.</p>




<h3 id="entity-configuration-scanning"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#entity-configuration-scanning" title="Entity Configuration Scanning">Entity Configuration Scanning</a></h3>
<p>The discovery problem compounds when you use <code>ApplyConfigurationsFromAssembly</code>. This method scans an assembly for all classes implementing <code>IEntityTypeConfiguration&lt;T&gt;</code> and applies them automatically. When the host scans a plugin assembly to load query configurations or read model conventions, it pulls in every entity configuration the plugin defines, regardless of whether those configurations were intended for the plugin&rsquo;s own context.</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// Looks tidy. Silently imports every plugin entity configuration.</span>
</span></span><span class="line"><span class="cl"><span class="kd">protected</span> <span class="kd">override</span> <span class="k">void</span> <span class="n">OnModelCreating</span><span class="p">(</span><span class="n">ModelBuilder</span> <span class="n">modelBuilder</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">modelBuilder</span><span class="p">.</span><span class="n">ApplyConfigurationsFromAssembly</span><span class="p">(</span><span class="k">typeof</span><span class="p">(</span><span class="n">BlogPlugin</span><span class="p">).</span><span class="n">Assembly</span><span class="p">);</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>Every <code>IEntityTypeConfiguration&lt;Post&gt;</code>, <code>IEntityTypeConfiguration&lt;Tag&gt;</code>, and <code>IEntityTypeConfiguration&lt;BlogCategory&gt;</code> in the plugin assembly gets applied. The host migration now owns every table those configurations describe. EF Core does not distinguish between configurations intended for the plugin&rsquo;s own context and configurations that happen to live in the same assembly. From the model builder&rsquo;s perspective, they are all equally valid type registrations.</p>




<h2 id="the-three-failure-modes"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#the-three-failure-modes" title="The Three Failure Modes">The Three Failure Modes</a></h2>
<p>Once the host migration owns plugin tables, you are dealing with three distinct failure modes, each painful in its own way.</p>
<p><strong>Duplicate schema creation.</strong> The plugin ships its own <code>DbContext</code> with its own migrations. When the plugin&rsquo;s migrations run, they attempt to create <code>blog.Posts</code>. The host already created it. The migration fails with a &ldquo;table already exists&rdquo; error, or worse, silently produces inconsistent state depending on how your deployment pipeline handles idempotency.</p>
<p><strong>Schema drift.</strong> The plugin&rsquo;s team adds a column to <code>Post</code>. They create a plugin migration. The host knows nothing about this column. It has its own snapshot, its own view of <code>Post</code> from whenever it first discovered the entity. The host migration model and the plugin migration model diverge. Now adding any unrelated host migration produces incorrect diff output, and the plugin schema change is either duplicated or lost depending on which context runs first.</p>
<p><strong>Orphaned migration history.</strong> The customer uninstalls the blog plugin. From a database perspective, the plugin tables should be removable. But those <code>CREATE TABLE</code> statements live in the host&rsquo;s migration history. You cannot cleanly roll back the plugin tables without touching the host migration timeline. And if you do touch the host migration timeline in a running production system, you risk breaking applied migration records for every deployed instance. The next deploy after an uninstall attempt is where this surfaces: the pipeline reapplies host migrations, expects <code>blog.Posts</code> to exist because the snapshot says so, and either fails outright or silently recreates a table the customer just removed.</p>
<p>These three failure modes share a root cause: <strong>the host migration timeline is coupled to the plugin&rsquo;s schema</strong>. Every change the plugin needs to make requires either coordination with the host migration sequence, or a workaround that increases complexity with every iteration.</p>




<h2 id="a-concrete-look-at-the-bad-migration"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#a-concrete-look-at-the-bad-migration" title="A Concrete Look at the Bad Migration">A Concrete Look at the Bad Migration</a></h2>
<p>Here is what a contaminated host migration looks like. Assume the host has a <code>CmsDbContext</code> with a <code>DbSet&lt;Page&gt;</code>. The plugin has a <code>Post</code> entity with an <code>AuthorId</code> property. The host also exposes <code>Post</code> via a <code>DbSet&lt;Post&gt;</code> property for dashboard queries.</p>




<h3 id="the-contaminated-dbcontext"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#the-contaminated-dbcontext" title="The Contaminated DbContext">The Contaminated DbContext</a></h3>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">public</span> <span class="k">class</span> <span class="nc">CmsDbContext</span><span class="p">(</span><span class="n">DbContextOptions</span><span class="p">&lt;</span><span class="n">CmsDbContext</span><span class="p">&gt;</span> <span class="n">options</span><span class="p">)</span> <span class="p">:</span> <span class="n">DbContext</span><span class="p">(</span><span class="n">options</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">DbSet</span><span class="p">&lt;</span><span class="n">Page</span><span class="p">&gt;</span> <span class="n">Pages</span> <span class="p">=&gt;</span> <span class="n">Set</span><span class="p">&lt;</span><span class="n">Page</span><span class="p">&gt;();</span>
</span></span><span class="line"><span class="cl">    <span class="c1">// Exposed for cross-context dashboard queries</span>
</span></span><span class="line"><span class="cl">    <span class="kd">public</span> <span class="n">DbSet</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;</span> <span class="n">Posts</span> <span class="p">=&gt;</span> <span class="n">Set</span><span class="p">&lt;</span><span class="n">Post</span><span class="p">&gt;();</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="kd">protected</span> <span class="kd">override</span> <span class="k">void</span> <span class="n">OnModelCreating</span><span class="p">(</span><span class="n">ModelBuilder</span> <span class="n">modelBuilder</span><span class="p">)</span>
</span></span><span class="line"><span class="cl">    <span class="p">{</span>
</span></span><span class="line"><span class="cl">        <span class="n">modelBuilder</span><span class="p">.</span><span class="n">HasDefaultSchema</span><span class="p">(</span><span class="s">&#34;cms&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="p">}</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div>



<h3 id="what-ef-core-generates"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#what-ef-core-generates" title="What EF Core Generates">What EF Core Generates</a></h3>
<p>Running <code>dotnet ef migrations add InitialCms</code> produces something like this in the generated migration:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="kd">protected</span> <span class="kd">override</span> <span class="k">void</span> <span class="n">Up</span><span class="p">(</span><span class="n">MigrationBuilder</span> <span class="n">migrationBuilder</span><span class="p">)</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">migrationBuilder</span><span class="p">.</span><span class="n">EnsureSchema</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="s">&#34;cms&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">migrationBuilder</span><span class="p">.</span><span class="n">EnsureSchema</span><span class="p">(</span><span class="n">name</span><span class="p">:</span> <span class="s">&#34;blog&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="n">migrationBuilder</span><span class="p">.</span><span class="n">CreateTable</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">name</span><span class="p">:</span> <span class="s">&#34;Pages&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">schema</span><span class="p">:</span> <span class="s">&#34;cms&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">columns</span><span class="p">:</span> <span class="n">table</span> <span class="p">=&gt;</span> <span class="k">new</span> <span class="p">{</span> <span class="cm">/* ... */</span> <span class="p">});</span>
</span></span><span class="line"><span class="cl">
</span></span><span class="line"><span class="cl">    <span class="c1">// The host has no business creating this table</span>
</span></span><span class="line"><span class="cl">    <span class="n">migrationBuilder</span><span class="p">.</span><span class="n">CreateTable</span><span class="p">(</span>
</span></span><span class="line"><span class="cl">        <span class="n">name</span><span class="p">:</span> <span class="s">&#34;Posts&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">schema</span><span class="p">:</span> <span class="s">&#34;blog&#34;</span><span class="p">,</span>
</span></span><span class="line"><span class="cl">        <span class="n">columns</span><span class="p">:</span> <span class="n">table</span> <span class="p">=&gt;</span> <span class="k">new</span> <span class="p">{</span> <span class="cm">/* ... */</span> <span class="p">});</span>
</span></span><span class="line"><span class="cl"><span class="p">}</span>
</span></span></code></pre></div><p>The migration file reflects EF Core&rsquo;s model snapshot. It does not know or care that <code>Post</code> logically belongs to the plugin. It just sees an entity type, and it creates the table.</p>
<p>The generated <code>CmsDbContextModelSnapshot.cs</code> is equally unambiguous about what it thinks it owns:</p>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-csharp" data-lang="csharp"><span class="line"><span class="cl"><span class="c1">// CmsDbContextModelSnapshot.cs (generated by EF Core)</span>
</span></span><span class="line"><span class="cl"><span class="n">modelBuilder</span><span class="p">.</span><span class="n">Entity</span><span class="p">(</span><span class="s">&#34;BlogPlugin.Entities.Post&#34;</span><span class="p">,</span> <span class="n">b</span> <span class="p">=&gt;</span>
</span></span><span class="line"><span class="cl"><span class="p">{</span>
</span></span><span class="line"><span class="cl">    <span class="n">b</span><span class="p">.</span><span class="n">Property</span><span class="p">&lt;</span><span class="n">Guid</span><span class="p">&gt;(</span><span class="s">&#34;Id&#34;</span><span class="p">).</span><span class="n">ValueGeneratedOnAdd</span><span class="p">();</span>
</span></span><span class="line"><span class="cl">    <span class="n">b</span><span class="p">.</span><span class="n">Property</span><span class="p">&lt;</span><span class="kt">string</span><span class="p">&gt;(</span><span class="s">&#34;Title&#34;</span><span class="p">).</span><span class="n">IsRequired</span><span class="p">().</span><span class="n">HasMaxLength</span><span class="p">(</span><span class="m">500</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">b</span><span class="p">.</span><span class="n">Property</span><span class="p">&lt;</span><span class="n">Guid</span><span class="p">&gt;(</span><span class="s">&#34;AuthorId&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">b</span><span class="p">.</span><span class="n">Property</span><span class="p">&lt;</span><span class="n">DateTime</span><span class="p">&gt;(</span><span class="s">&#34;PublishedAt&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">b</span><span class="p">.</span><span class="n">HasKey</span><span class="p">(</span><span class="s">&#34;Id&#34;</span><span class="p">);</span>
</span></span><span class="line"><span class="cl">    <span class="n">b</span><span class="p">.</span><span class="n">ToTable</span><span class="p">(</span><span class="s">&#34;Posts&#34;</span><span class="p">,</span> <span class="s">&#34;blog&#34;</span><span class="p">);</span> <span class="c1">// host snapshot permanently records this</span>
</span></span><span class="line"><span class="cl"><span class="p">});</span>
</span></span></code></pre></div><p>This snapshot entry is what makes the problem persistent. Even if you remove <code>DbSet&lt;Post&gt;</code> from <code>CmsDbContext</code> tomorrow, EF Core will detect the discrepancy and offer to generate a DROP TABLE migration. The snapshot is the source of truth for what the host context &ldquo;owns,&rdquo; and once a plugin entity is in it, extraction requires deliberate intervention.</p>




<h2 id="the-model-snapshot-trap"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#the-model-snapshot-trap" title="The Model Snapshot Trap">The Model Snapshot Trap</a></h2>
<p>EF Core&rsquo;s migration system works by diffing the current model against a stored snapshot. That snapshot lives in <code>CmsDbContextModelSnapshot.cs</code> next to your migration files, and EF Core generates and updates it automatically every time you run <code>dotnet ef migrations add</code>.</p>
<p>Once a plugin entity appears in a migration, it enters the snapshot. Removing the <code>DbSet&lt;Post&gt;</code> property from <code>CmsDbContext</code> does not clean the snapshot. When you run the next migration (even one completely unrelated to posts), EF Core diffs the new model (no <code>Post</code>) against the snapshot (has <code>Post</code>) and generates a <code>DROP TABLE [blog].[Posts]</code> statement. If that migration reaches production, you have deleted the plugin&rsquo;s data.</p>
<p>The snapshot trap makes contamination sticky. You cannot simply stop referencing the plugin entity and move on. You have to either:</p>
<ol>
<li>Keep the entity in the model and accept permanent coupling to the plugin&rsquo;s schema</li>
<li>Surgically remove the entity from both the migration file and <code>CmsDbContextModelSnapshot.cs</code>, a manual edit that is easy to get wrong and leaves the history in an inconsistent state</li>
<li>Use <code>ExcludeFromMigrations()</code> before the entity ever enters the snapshot, which prevents DDL ownership from being recorded in the first place</li>
</ol>
<p>The third option is the only one that scales across multiple plugins and multiple schema versions. Once the snapshot contains a CREATE TABLE for a plugin table, you are already in recovery mode. The clean path is to never let it get there.</p>




<h2 id="why-the-obvious-fixes-fall-short"><a href="/posts/efcore-excludefrommigrations-1-the-problem/#why-the-obvious-fixes-fall-short" title="Why the Obvious Fixes Fall Short">Why the Obvious Fixes Fall Short</a></h2>
<p>The instinctive response is to reach for <code>modelBuilder.Ignore&lt;Post&gt;()</code> or the <code>[NotMapped]</code> attribute. Both work, in the sense that EF Core stops generating migrations for the entity. But they also strip EF Core&rsquo;s awareness of the type entirely. You can no longer query <code>Posts</code> through the <code>CmsDbContext</code>. If your dashboard needs to join pages with posts, you have lost EF Core&rsquo;s ability to help you do that.</p>
<p>Take the dashboard scenario from earlier as a concrete case. A &ldquo;recent activity&rdquo; widget needs pages joined with their latest posts, filtered by author and ordered by publish date. With <code>Post</code> mapped, that is one LINQ query. Call <code>Ignore&lt;Post&gt;()</code> and that query is gone: you are left writing raw SQL against <code>blog.Posts</code>, or standing up a second <code>DbContext</code> and stitching results together in memory. Both cost real maintenance effort for a problem unrelated to the dashboard feature.</p>
<p><code>HasNoKey()</code> combined with <code>.ToView(&quot;Posts&quot;, &quot;blog&quot;)</code> is closer: the entity becomes read-only and EF Core skips migration generation for views. But it changes the entity&rsquo;s semantics to a view mapping, which is semantically wrong (it is a table), and it blocks insert/update/delete scenarios you might legitimately need.</p>
<p>What you actually want is a way to tell EF Core: &ldquo;know about this entity, keep it in the model, let me query it, but do not touch its table in migrations.&rdquo; That precise capability exists. It is a single method call, and it keeps the full EF Core query surface intact while handing schema ownership back to the plugin. That is the subject of Part 2 of this series.</p>
]]></content:encoded>
      <enclosure url="https://daily-devops.net/images/extensions.png" type="image/png" length="0"/>
      <media:content url="https://daily-devops.net/images/extensions.png" medium="image"/>
      <category>efcore</category>
      <category>dotnet</category>
      <category>csharp</category>
      <category>architecture</category>
      <category>plugins</category>
    </item>
  </channel>
</rss>
